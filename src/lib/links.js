/*
 * Outward links PRSH points a producer at, in one place.
 *
 * These are the answers to "where do I GET that?" — questions the app used to
 * raise and not answer. The MSB image pack is the load-bearing one: PRSH
 * declares it required in three surfaces (the Welcome checklist, the
 * Connections card, the README) and shipped none of them with a source, so a
 * new user hit a hard requirement with nowhere to go. PRSH can't ship the
 * images (Nintendo IP) — pointing at the community that has them is the whole
 * of what it CAN do, and it costs a hyperlink.
 */

/** The Mario Superstar Baseball community Discord — where the image pack lives. */
export const MSB_DISCORD_URL = 'https://discord.gg/WfkYyaHBEu';
