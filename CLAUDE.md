# CLAUDE.md — ProjectRioStreamHelper (PRSH)

## Project Overview

A web-based tournament stream overlay manager for **Mario Superstar Baseball (MSB)** via the Project Rio mod. Forked from TournamentStreamHelper (originally Qt/PySide6) and rebuilt as a single-game web app.

**Server:** `server/` — FastAPI + SocketIO on configurable port (default 5260).
**Frontend:** `src/` — React SPA served by FastAPI in production, Vite dev server in development.
**Builds:** PyInstaller (`PRSH.spec`) → standalone macOS `.app` / Windows `.exe`; Inno Setup (`installer/PRSH.iss`) → Windows installer.
**Submodules:** `server/rio/pyrio` (MSB data + stat parsing), `gc-overlay/` (controller overlay), `rio-visualizer/` (hit-trajectory simulation).

See [README.md](README.md) for end-user docs and [TESTING.md](TESTING.md) for the test-suite design.

## Agent Quickstart

```bash
./venv/bin/python -m pytest        # backend suite (~13s)
npm run test:run                   # frontend suite (vitest)
npm run vite:lint                  # ESLint — src/ + public/layout/lib/ (flat config: eslint.config.js)
npm run build                      # frontend production build
npm run dev                        # Vite (5173) + FastAPI (5260) together
```

- `python main.py` from a source checkout runs the server **headless** (tray/Tk UI is gated on frozen builds, not dev mode). ⚠️ With no overrides it uses the real `./user_data/` — for verification, boot an **isolated instance** via `PRSH_USER_DATA_DIR` + `PRSH_PORT` + `PRSH_NO_BROWSER` + `PRSH_HUD_FILE`, and drive the HUD pipeline with `scripts/replay-hud.py` (see the `run-and-verify` skill).
- Layout HTML and `public/layout/lib/*.js` are static — no build step, and **PRSH now serves them `Cache-Control: no-cache`** (`server/http_cache.py`), so an ordinary reload picks up an edit. A hard refresh is still the fastest way past a cache poisoned *before* that shipped.
- CI (`.github/workflows/test.yml`) runs both suites on PRs and pushes to `main`/`2.0.0`.
- **Deep-dive skills** in `.claude/skills/` — this file is the map; the skills hold the depth. Load the matching one before working in its area: `state-keys-and-projectors` (State contract, namespaces, projector pattern), `match-binding-lifecycle` (fixtures, bindings, side cascade, game end, start.gg), `overlay-authoring` (layouts, mounts, themes, OBS behavior), `design-package-authoring` (theme SVG packages, converting designer exports), `production-console-contract` (Production rack/stage/rail + element contract), `controller-overlay` (gc-overlay subprocess + per-side follow), `run-and-verify` (tests, booting, smoke recipes).

---

## Glossary

These terms have specific meanings in this codebase. Use them precisely; correct an agent that drifts.

| Term | Meaning |
|------|---------|
| **Scoreboard / Board** | One entry under `score.{N}` in state, listed in `scoreboards.active`. N ≥ 1; multiple may be active simultaneously. At least one always remains. Its console home is a **board desk** (`desk:board:{N}`) — added and removed from the rack's own `BOARDS` section, renamed on its stage panel. |
| **Binding** | What fills a scoreboard: `scoreboards.binding.{N}` in **Settings** — a `pool` (membership) + `playback` (how the pool is shown) + `stats_tag`. Defined in `server/bindings.py`. There is **no "source" enum** — "manual" is just the empty binding (single playback, empty pool). |
| **Pool** | A binding's game membership: `filters` (search chips: tag/username/vs_username/limit), `scope` (`live \| completed \| both`), `pinned` (always in), `excluded` (never in), `refresh_interval` (0 = static). |
| **Playback** | How a board presents its pool: `mode: single \| rotate`, `gameId` (single: null = auto-follow), `interval` + `running` (rotate). |
| **Transport** | HUD file vs API — **derived, never user-picked**: board 1 carries the local HUD iff the global `project_rio.hud_enabled` toggle is on; every other board is API. `bindings.transport(sb)`. |
| **Side** | **1 and 2, never left and right.** The model is `score.{N}.player.{T}`, `?team=1\|2`, `scope.team` — a side is a number everywhere in state, in URLs and in this file. Never use "away/home" in app vocabulary (Project Rio uses those internally; we translate), and never bake "left"/"right" into a label: what the console *calls* a side is a producer setting (`production.side_labels`, `src/routes/production/sides.js`) whose default is the numbers. Left/Right is a fact about one arrangement of one scene. |
| **Team** | Synonymous with Side in state (`.player.{T}` where T∈{1,2}). |
| **Match** | The fixture object above scoreboards: `match.{M}` in state (participants per side, captain, format, series, stage `draft \| live \| post`). A board binds to it via `score.{N}.match = M` (int id, never a round-name string). **"Fixture" is prose here and NEVER appears in the UI** — it is the precise word for the thing (an authored pairing that exists before and after any game), and it is a word the MSB community does not use, which is what settles it: the community has no *match* concept either, so the label has to be self-evident cold and "Match" at least matches the state key, the desk, the tab that was, and start.gg. Retired from every rendered string on 2026-08-30 (`NO FIXTURE` → `NO MATCH`, `Fixtures from` → `Matches from`, the Match desk's `Fixture` field → `Match`); comments and identifiers keep it, because there the distinction from a *game* is the whole point. |
| **Queue / Running order** | One entry in `schedule.queues` — `{id, title, matches}`, an **ordered list** of match ids and only an order: there is no stored position, because several matches run live at once. **There may be several** (a winners order and a losers order), and a board draws from one (`scoreboards.match_queue.{N}`, unassigned = the first). Membership is **exclusive** across orders — a fixture belongs to one, so naming another *moves* it. A board takes the next fixture *waiting for one* (queued, unbound, undecided, never started). Creating a match **enrols** it at the end of an order — the one named by `POST /match?queue={qid}`, or the first when none is; membership and position are authored on the **Match desk**, whose accordion stack *is* these orders and whose per-order **`+`** is how a fixture enters one. Not to be confused with **Rotation**, which cycles one board's game pool. |
| **`schedule.queue`** | A **projection**, not a model: the union of every order in order, rewritten in the same `SetBatch` as `schedule.queues`. It is what the schedule overlay and the console subject read — the union deliberately, so creating a second order never silently drops a fixture off air. Never write it directly. `schedule.title` is the **overlay's heading**, independent of any order's title. |
| **Projector** | The resolve-by-copy pattern: a model (Match, Commentary, PlayerPlates, PostGame, Organizers) resolves its records against the participant registry and copies the result into the state keys overlays read. Deterministic: writes the full key set (value or `""`) so unbinding blanks exactly what it set. |
| **Placement** | A Production rack row: one source, in one scene — `{element}:{board}@{scene}`. The console's row identity; see `src/routes/production/placements.js`. |
| **Element** | A Production-page broadcast unit. **Direct** elements own a dedicated source; **fed** elements have no source of their own and can only be pushed into a container. Being fed is a property of the **placement**, not the element: a member on a container's roster rows under it and pushes into it, while that same element's own source rows on its own and just shows/hides (`placementFlavor`, `src/routes/production/placements.js`). Related: **dedicated source** / **feed** — see `src/routes/production/elements.js`. |
| **Container** | One OBS browser source that hosts whichever of its **members** is fed to it. Producer-built: `settings.production.container_defs.{id}` = name + native size + member roster, all rendered by one shell (`/layout/shared/container.html?container={id}`). Membership lives on the container and is **exclusive** — `production.feed.container.{id}` holds exactly one occupant, so sharing a container is what "these never appear together" means. Where each member sits is the container's too (`offsets.{member}` = `{anchor, x, y}`, absent = centered). Container-scoped members are the exception (see **Member**). |
| **Member** | An element on a container's roster. Must fit the container (same size or smaller — smaller ones center, nothing ever scales). A **container-scoped** member (`containerScoped`: Roster, Stat Card, Controller) has no content of its own — it draws whoever the container's scope has on the field — so it is the one kind that may sit on several rosters at once. |
| **Automation** (container) | A rule that makes a container feed itself: `trigger` (a state key changes) · `guard` (the content resolves) · `action` (show a member) · `dwell` · return to **resting**. Stored in `settings.production.automations.{id}`, run by `server/automations.py` **inside the state-write path**. Precedence **manual > rule > resting**, mirrored to `production.feed.reason.{container}`. Not to be confused with an **Announcement**-style notification, and unrelated to *Rotation*. |
| **Resting occupant** | What a container shows when nothing else is up — `container_defs.{id}.resting`. The state a dwell returns to and the boot state. Absent = empty (transparent), which is right for a container that only ever holds pushed content. |
| **Container scope** | `container_defs.{id}.scope` = `{scoreboard, team}` — a container's frame of reference. Resolves `{sb}` in a rule's trigger and picks the side of the content the engine feeds; a mirrored pair is two scoped containers running one canned rule. |
| **Layout** | An HTML file under `public/layout/`, served as an OBS Browser Source. Declares its style contract via `<meta name="overlay-settings">` and its native size via `body { width/height }`. |
| **Layout type** | Derived from filename + group folder (`server/api/v1/layouts.py`). Drives `?size=` / `?team=` variant expansion. |
| **Scene** | An **OBS** scene — the Production rack's grouping axis. PRSH has no scene *Layout*: the "full scene" group (a 1920×1080 layout that was the entire stream canvas) was dropped, because a canvas built from placed elements is what the console is for. |
| **Overlay** | Generic OBS Browser Source terminology. Not PRSH-specific; do not use as a synonym for any of the terms above. |
| **Sample bundle** | A Layout's canned state fragment under `public/layout/preview/*_sample.json`, declared via `OverlayBase.init({ sample })`. Every Layout has one. Keys carry `{sb}`/`{team}` tokens resolved from the page URL. |
| **Demo mode** | State `production.sample` — the app-wide switch that makes every overlay render its sample bundle instead of live state, for building OBS scenes with no game running. Global, never self-enabling, guarded by an app-wide banner. |
| **Size variant** | `?size=` query param, scoreboard layouts only. Which sizes exist and their canvases live in `server/theme_contracts.py` (`CONTRACTS`) — the one source of truth, pinned across runtimes by `tests/unit/test_size_dims_parity.py`. The mount resolves any unknown or retired size to `l`, so legacy OBS sources keep working — which is what makes **retiring a size a supported move**: `xs`/`xl` went with the SVG conversion and `m` (600×200) on 2026-08-29. **Two sizes remain, and the gap between them is the point**: `s` is a compact pill (names · score · count), `l` the full card (rosters + linescore, in BOTH states). M had no content of its own — only L's at a smaller scale — so every improvement to either end pulled it back toward L until the two were the same card 200 units apart. A new size has to be able to say what it shows that its neighbours don't. |
| **Team variant** | `?team=1\|2` query param. Applies to stats, roster, teamlogo, controller, playername — the five elements that come in PAIRS. Each declares `perSide` (`src/routes/production/elements.js`) and appears in `_TEAM_VARIANTS` (`server/api/v1/layouts.py`): the first rows both sides in the console's catalog tier with OBS closed, the second offers both in the Add picker. One fact, two runtimes, pinned by `tests/unit/test_per_side_parity.py`. Unlike a size variant there is **no default row** — every source names its side — so the catalog emits `~t1` and `~t2` and never a bare id. |
| **Rotation / Rotating** | A board whose playback mode is `rotate`, cycling its pool at an interval. Managed by `PoolManager` (`server/rio/rotation.py`). No other meaning — there is no "player rotation" concept. |
| **Rotator (layout group)** | `public/layout/rotator/*.html` — standalone layouts (e.g. the results ticker) that display rotating content. Distinct from rotate playback. |
| **Design package / Theme** | A swappable SVG theme set under `public/design/{package}/` (default, classic, + user packages in `user_data`). Elements mount theme SVGs via `svg-theme-engine.js` data-slot binding. |
| **HUD game** | Game data sourced from the local `decoded.hud.json` file. |
| **API game** | Game data sourced from `https://api.projectrio.app/`. |
| **Participant / Address Book** | The participant registry (`server/participants.py`, REST-only, `participants.json`): rio name → display identity (name, team, pronouns, socials). The join point for Match, Commentary, PlayerPlates, Matchup and the competition's Organizers. |
| **Book / League book** | The Address Book holds several **books** (`participants.json` schema v2): every row is in exactly one (`row.book`), and `main` is the undeletable one every install has. A book other than main may be linked to a **league** — the Rio game modes it is played in (`book.modes`, by NAME) — and each person in it may carry a **logo** (`row.logo`, a file under `user_data/branding/leagues/{book}/{pid}.{ext}`). **There are no teams**: in the league this was built for every team is one manager, so a team was a second name for a person, and its name's one on-air use (Player Name's tag line) is already the league row's own sponsor `prefix`. A book is the unit a producer SHARES, as a **`.prsh-book.zip`**: `book.json` (league + people, each row's `logo` a path inside the zip) beside a `logos/` folder of real image files — not base64 in the JSON, which was a third bigger and unreadable. Not a *Preset*. |
| **Pinned player** | A participant with a **preferred side** — `prefs.side` (1 or 2) on their Address Book row, which PRSH forces onto that Side at the start of each new game. It is the `pin` layer of the side cascade, read by `pin_swap`. A pin is a fact about a **person**, not about the app: it was one app-wide `project_rio.pinned_player`/`pinned_side` pair until 2026-08-28, and one global pin was an implementation ceiling rather than a decision. Authored in the **Side** column of the Address Book. |
| **Stream labels** | Per-key `.txt` mirror of state under `user_data/stream_labels/`. Off by default. Intended for OBS Text Sources. |
| **Branding** | Tournament logo + merch assets uploaded by the user to `user_data/branding/`, served at `/branding/`. |
| **Preset** | A saved, swappable design — `overlays.presets.{id}` (v3), authored on the Design tab. Holds the **design package, every global design key, every per-element style pin at any namespace depth, each element's own palette settings, and the logo**; never element CONTENT (bands, sizes, what a card shows). **Applying a preset REPLACES what it covers** (`planApply`, `src/routes/layouts/presets.js`): pins it doesn't carry are unset, in one `Settings.ApplyBatch` commit. The legacy (pre-v3) presets merged, so tournament B inherited A's pins. `overlays.active_preset` names the one applied; "Modified" is computed, never stored. **Called "Look" until 2026-09-18** — renamed because the producer wants that word for something else; `Settings.Load` moves `overlays.active_look` and `branding.py` moves `branding/looks/` on first use. |
| **MSB assets** | User-supplied image pack at `user_data/game_assets/msb/{characterIcons,teamLogos,gameIcons}`. Validated against pyrio's canonical filename lists. PRSH does not ship these (Nintendo IP). |
| **Announcement** | Notification surfaced from GitHub: either an "Update available" synthesized from the Releases API, or an entry in `announcements.json` on the repo's `announcements` branch. |

---

## Architecture

### Core Data Flow

```
Project Rio game ─→ decoded.hud.json ─→ HudWatcher ─→ RioGameDataProvider ──┐
Project Rio API ─→ Ongoing/CompletedGamePool ─→ PoolManager (bindings) ─────┤
start.gg ─→ StartGGProvider ─→ Match (fixture) ─┐                           │
Participants registry ──────────────────────────┤                           ▼
Commentary / PlayerPlates / Matchup / Schedule ─┴─ projectors ──→ State.SetBatch()
Post-game stat files ─→ StatFileWatcher ─→ PostGame capture ─────────↗      │
                                                                            ▼
                                              SocketIO (v1.state.set/set_batch) + v1.action bus
                                                            │
                                              React UI + OBS overlays (+ optional stream_labels/)
```

Two write paths into `score.{N}.*` meet at the board: the **live feed** (HUD/API game data) and the **Match projector** (authored fixture data). The side cascade (below) and the projector's key ownership rules keep them from fighting.

In **frozen builds**, `user_data/` lives under the per-user writable root resolved by `server/paths.py` (e.g. `~/Library/Application Support/PRSH/user_data/` on macOS, `%LOCALAPPDATA%\PRSH\user_data\` on Windows). In **dev mode** it's `./user_data/` next to the repo.

### Singleton Pattern

The server uses class-level singletons with `@classmethod` methods, state shared via class variables — `State`, `Settings`/`Config`, `RioGameDataProvider`, `StatsTracker`, `PoolManager`, `Match`, `Participants`, the element models, and the watchers all follow this shape. Match it when adding a new server-side model; don't introduce instance-based services alongside it.

---

## State — The Central Nervous System

Every change flows through `State.Set(key, value)` or `State.SetBatch(entries)`. This:
1. Updates the in-memory `state` dict.
2. Tracks changed keys in `changed_keys`.
3. On `Save()`: compares only tracked keys against `last_state` (no full-state diff).
4. Exports changed keys as individual `.txt` files (if enabled).
5. Broadcasts via SocketIO (`v1.state.set` or `v1.state.set_batch`).

**Key patterns:**
- `State.Set("score.1.player.1.rioName", "Player1")` — single key.
- `State.SetBatch([(k, v), ...])` — multiple keys; **always prefer this when writing more than one key**.
- `State.Save()` — diff from tracked keys only.
- File export off by default (`general.disable_export: True`).

**Scoreboard config lives in Settings, not State.** `scoreboards.active`, `scoreboards.aliases`, `scoreboards.binding.{N}` and `scoreboards.match_queue.{N}` are **Settings** keys. Legacy `scoreboards.sources` / flat `scoreboards.rotation` settings are read-only migration fallbacks — never write them.

**A new per-board KEY — state or settings — must be torn down in BOTH `remove_scoreboard` and `POST /scoreboards/reset`.** Board ids are re-used (`_lowest_available_id`), so a leftover isn't dormant, it's inherited by the next board with that id. Read that as per-board **data**, not per-board *setting*: stating it as a settings rule is exactly how `postgame.{N}` — per-board state that doesn't live under `score.{N}`, so `State.Unset("score.{N}")` never reached it — sat outside the teardown while all four settings keys were handled right. The two paths also drifted into opposite halves of the same pair (see below), so **fix both or neither**; `tests/integration/test_scoreboards_api.py` pins the whole set against id re-use.

**`scoreboards.rotation.{N}` is TWO keys in two stores.** In **State** it's the live status mirror (`running` / `game_ids` / `cached_games`, written by `PoolManager`) that the rack badge reads; in **Settings** it's the legacy flat config. Both must go when a board does — and `PoolManager.stop_rotation` clears neither, it drops the task and the binding flag, not the projection. Don't "simplify" either call site to a single store.

> Deep dive: `.claude/skills/state-keys-and-projectors/SKILL.md` — the full state-key namespace map, the Settings-vs-State split, and the projector pattern.

---

## Scoreboards & Bindings

- Each board's binding is independent; boards can share pool contents.
- **Transport is derived**: board 1 carries the local HUD iff `project_rio.hud_enabled`. There is no per-board source selector — don't add one.
- Boards can be renamed (alias) and removed; at least one always remains. Layouts bind via `?scoreboard=N` — **a missing param defaults to board 1**, which is safe because board 1 always exists.
- `POST /scoreboards/reset` is the escape hatch that clears stale board/binding state.

### Player-Side Cascade (who sits left/right)

Project Rio randomly assigns away/home each game. `_decide()` in `server/rio/provider.py` resolves each board's orientation with precedence:

**manual > match > pin > back_to_back > none**

**The `pin` layer is ONE function, `pin_swap` (`server/rio/provider.py`)**, over each person's `prefs.side` in the Address Book — imported by the HUD cascade, the API pools and the pool endpoint. It was two copies of the rule reading the same settings pair, agreeing only because neither had changed since it was written. Because a pin now belongs to a **person**, both players can carry one: opposite sides means they agree, and **the same side is unsatisfiable, so the layer abstains** and the cascade falls through rather than picking an arbitrary winner. `Participants.adopt_legacy_pin` migrates the old global pair onto its person once, at boot.

**`manual` is the only layer with an EXIT, and it needs one.** `release_sides_override` (`server/rio/provider.py`, `POST /rio/swap/release`) clears `_user_overridden` and re-settles the boards; the board desk offers it as **Use auto** beside the swap, in the same idiom as the game mode's override. Until it existed the flag cleared on a new game, or mid-game if a *second* swap happened to land on exactly what the pin already wanted — neither of which is something a producer can ask for, so a swap made to fix one frame (or made before a fixture was bound) outranked that fixture for the rest of the game. Two things it is not: **a release is not a flip** — manual decides every board the same way, so the toggle carries display identity across all of them, while the cascade underneath can hand one board to a match and leave the board beside it where it is (carry per board, on the ones that actually move); and it **reseeds `_sides_swapped` from the non-manual cascade**, the same reseed a new game does, so the next swap flips from what is on air rather than from the orientation just abandoned. On a released feed or with no frame it drops the flag and touches nothing — handing the sides back is not a request to bring a reset game with them (the rule `reorient_board` already follows).

The deciding layer is mirrored to `score.{N}.side_reason`. Manual scope is the current game only. Tests encoding this live in `tests/unit/rio/test_side_preservation.py` — change behavior and tests in the same commit.

### Match Model & Projectors

- `apply_startgg_set` in `server/api/v1/match.py` is the **only** set→match path. There is no direct set→score path.
- **A match fills exactly one board**, and `bind_board` (`server/api/v1/match.py`) is the only writer of `score.{N}.match` — binding a match another board holds *moves* it, vacating and blanking the old holder. Never write that key directly; the invariant used to live in a click handler and every other caller missed it.
- **BINDING IS AUTHORING; TAKING IS TURNOVER — ONLY THE SECOND CLEARS THE BOARD'S GAME.** `_clear_superseded_game` ran inside `bind_board`, so every path that attaches a fixture inherited it: the Match desk's board chips, the board desk's bind, the start.gg set loader. On a HUD board the game on screen is whatever Project Rio is showing, and saying "this game is Match 5" is the ordinary working gesture — so attaching a match **blanked the producer's scoreboard**, with nothing asked for and nothing said. It is `bind_board(..., supersede=True)` now, and `take_next_match` is the only caller that passes it, because that verb's whole meaning is "this board is done with what it has" and its button says so. What the auto-clear was originally hired for is solved better elsewhere: the real defect behind "a new match attaches to the previous scoreboard" was the PROJECTOR deferring to stale feed data and dropping its picks, which `_board_side_has_feed_data` answers by not treating a `restored` board as a live feed; what is left — last night's score under tonight's names — is visible, and has an explicit prominent verb on the turnover bar. **A button the producer chooses beats an invisible rule that sometimes eats their game.**
- **The queue is an order, not a cursor.** `POST /scoreboards/{sb}/next-match` resolves the next waiting fixture and binds it under one lock. Eligible = queued ∧ unbound ∧ undecided ∧ `stage == 'draft'` — stated **once**, in `Schedule.not_waiting_reason`, which returns the producer-facing reason and of which `is_up_next_eligible` is just `... is None`. Never reimplement the four conditions; the verdict and the explanation must not be able to disagree. Several matches run live at once, so **never** add a stored position — see `server/schedule.py`.
- **`stage` is producer-settable, and has to be.** The server only ever moves it forward (`note_live` → `live`, post-game → `post`), so a fixture fed once and then unbound was stranded out of Up next forever by the anti-bounce condition. The Match desk's stage badge is a control (`StageControl` in `desks/match.jsx`) that can send it back to `draft`, and states which condition is holding a fixture back.
- **A DOUBLEHEADER IS A FORMAT, NOT A SECOND KIND OF FIXTURE** — `format.bestOf = 2`, offered as **`DH`** on the Match desk (second in the list, because it is the common repeat and a Bo3 is the rarity). No new field and no second code path: the clinch majority every caller already runs (`bestOf // 2 + 1`) reads 2 as *win both*, so a sweep decides it and **a 1-1 split correctly never does** — a split has no winner. Only the NAME is new, and **no surface may print `Bo2`**: that is the arithmetic's word for a thing that cannot exist. One module, two runtimes — `public/layout/lib/match-format.js` (`FORMATS` · `formatLabel` · `gamesToWin` · `seriesContinues`), imported by the console the same way `container-members.js` and `spotlight-intent.js` are, because the schedule card and the format picker must not disagree. **A DOUBLEHEADER IS COMPLETE AFTER TWO GAMES HOWEVER THEY FALL**, which splits one question into two: `decided` records a WINNER, `matchComplete`/`Match.is_complete` records the END. They agree for every odd format (somebody always clinches) and come apart only here — a 1-1 **split** is finished and unwon. Everywhere `decided` was standing in for "this fixture is finished" now asks completeness instead: the handover (`slotTake`), the take's stand-down (`seriesContinues`, which counts games played), the clear that unbinds (`releaseMatch`), the identity gate's auto-retire (`Match.gate_for_sides`), `Schedule.not_waiting_reason`, and the Upcoming Schedule's played filter — each of which read a split as still running, leaving a finished fixture bound to its board, offering no handover, and sitting on an "Upcoming" card all night. The badge says **`SPLIT`**, never `DECIDED`, and dims neither name: there is no winner to name — on the board and on the Match desk, which also drops the lifecycle badge and `Ready next game` there, because a split is finished and those two answer "why is this not coming up?". **A SPLIT IS COMPLETE, NOT STUCK, AND THE DESK OFFERS NO VERB TO INVENT A WINNER** — no `Decide`, no `Reopen`, no stage badge, no `Ready next game`; the badge states it and everything that moves a fixture on stands down beside it. A pick between the two participants was built here and removed the same day: a doubleheader does not owe anyone a winner, and the only honest correction to a split is to the SERIES, on the steppers. **The verbs read the STAGED series**, via `draft.val` like every other control on the desk: `clinchedSide` read live state while the stepper wrote into the staging buffer, so under confirm mode bumping a side to the win count turned the score amber and offered no Decide until the bump was committed — two commits for one intent, the button invisible in between. **Both runtimes' waiting tests ask completeness** — `Schedule.not_waiting_reason` AND its client mirror `notWaitingReason` (`src/routes/production/queue.js`). Moving only the server left the console offering a played split back to a board as fresh (`UP NEXT` + `Put on board` on a fixture with no games left in it) and promising a take the server would then refuse — the two are mirrors on purpose, so a change to one is a change to both. start.gg never produces one: `apply_startgg_set` bumps an even `totalGames` to the next odd number, so a DH is always something a producer set up by hand. `src/routes/layouts/match-format.test.js` + the DH cases in `test_postgame_series_credit.py`.
- **A Bo1 IS THE APP'S ONLY ASSUMPTION.** `default_match()` is `{"bestOf": 1}` and essentially every night is one — the common *repeat* is a **doubleheader**, which is two Bo1 fixtures back to back and rides the ordinary capture → decide → **take next** path, not a series. So nothing may state, imply, or wait on a further game unless the FORMAT says one can follow (`bestOf > 1`, set by hand on the Match desk). `between games` was gated on `stage === 'post'` alone and so turned up on Bo1 boards, where it describes no state at all — there is no next game to be between — and it did so **exactly when a capture failed to credit a winner**, i.e. when the producer was already asking what happened to their match. Series width (`w1–w2`), the `GAME 2 OF 3` badge and the take's `seriesContinues` stand-down are all `bestOf > 1` for the same reason.
- **The match owns the series** (games won within the Bo format); boards own only their live game. **A SERIES NEVER HOLDS MORE GAMES THAN ITS FORMAT ALLOWS**, enforced at the two places a game is added and nowhere else: `Match.award_game` refuses to credit a capture once `is_complete` (checked AFTER the `game_id` dedup, or the last game of a series would stop being idempotent), and `_check_series_fits` refuses a `PUT /match/{m}` that lengthens the series past `bestOf`. Both matter because a board **keeps its binding** until the producer clears it or hands it on, so the game after a finished fixture arrives on a board still bound to it — crediting that pushed a doubleheader to 2-1 and a Bo1 to 2-0, inventing a game the format says cannot exist on a count a bound board puts on air. Stated as "may not INCREASE past the format", not "may not exceed it": the two come apart on LOWERING `bestOf` under a longer series (a Bo3 at 2-1 set back to a Bo1), which is not adding a game, and refusing the correction out of it would trap the producer in a format they cannot leave without first zeroing a score that is on air. The desk's steppers cap themselves against the same ceiling so nobody reaches the refusal — and for that same reason they **never clamp DOWN**: out of room is a press that does nothing, because a `+` that answers by deleting two games is worse than a `+` that is spent.
- **Identity gate:** if live players don't match the bound fixture, `score.{N}.match_conflict` raises the app-wide banner; a decided-mismatch auto-retires the binding.
- **Projector rules** (Match, Commentary, PlayerPlates, PostGame, Organizers all follow this shape — reuse it, don't invent a new one): resolve records against the Participants registry, write the *full* owned key set via `SetBatch` (value or `""`), wrap `project_all()` startup hooks in try/except so a bad record never blocks boot (use `run_startup_projection`).
- **A FEED-SHARED key is written with a value, never blanked over live data.** `score.{N}.player.{T}.*` is the one place a projector and a live feed both write, and for the Match projector the overlap is *total* — every key it owns is also fed (`_FEED_SHARED_KEYS`, `server/match.py`). Full-key-set determinism still applies to a board with no game; over a board carrying one, an empty projected value defers. This began as a two-key captain carve-out, and the two keys it left out were the ones that name the player: unbinding mid-game blanked both sides' `rioName` while the board kept its teams, rosters and inning, so the Quick Rail said "No game on this board yet" for a board at inning 9. Deferring on *every* empty value is the mirror-image bug, so the rule turns on whether a blank is an **answer**: an address-book field is one once a participant resolves (re-binding to someone with no twitter must clear it), while `port`/`rio_captainIndex`/`character.0.name` never are (`_OPTIONAL_PICK_KEYS`). The discriminator for "is there live data here" is `score.{N}.game_id` **and the board not being `restored`** — a projection's own output populates the side, so asking "is the side populated" strands the fixture an unbind is supposed to clear. `game_id` alone answers it for every game a producer is DELIBERATELY SHOWING (live, stranded mid-frame, or a completed record on screen as content — all three have real ports, a real captain and real names that must outrank a draft pick). What it cannot answer is a game that is on the board because it was on the board when PRSH last exited: a finished game keeps its id forever, so the cold start bound tonight's fixture over last night's players, deferring every blank to them and dropping both picks. **That is the ONLY narrowing** — do not widen it to `is_stale`, which breaks the completed-game rule the port carve-out exists for. The turnover case (a game that ended *this session*) is answered by the TAKE clearing the game instead, which is truer than reasoning about whose it was — see the binding row below for why that clear belongs to `take_next_match` and not to every bind.
- **A PICK that stands in for feed data is written only when there is none.** The two are different rules and the second is not a special case of the first: the feed-shared rule reconsiders a key the projector left **empty**, while a stand-in is wrong exactly when the projector *has* a value. The Match projector's captain pick (`character.0.name` + `rio_captainIndex = 0`, mirroring the Live-API "captain only, no roster" shape) is the one that exists, and over a live game it wrote the drafted character into roster slot 0 and forced the index to 0 — so every overlay that resolves the captain BY INDEX (the scoreboard's team-logo fallback, the Game Summary hero art) drew slot 0 and named the wrong character for the whole broadcast, while `msb_team` beside it, derived by the feed from the real captain, disagreed. Gate it on `_board_side_has_feed_data`, not on the deferral loop — **and on `_side_has_roster`**, because that discriminator is deliberately False on a `restored` board (so tonight's names and port win over last night's), and the captain keys rode along: binding a fixture with no captain over last night's game blanked slot 0 and the captain index of a real nine on both sides, a hole on air. The projector only ever writes slot 0, so anything in slots 1..8 is a feed's roster and the captain keys are not the projector's there — neither the pick nor the blank. `tests/unit/test_match_projection.py` pins both halves.
- **A fixture mutation must RE-SETTLE its bound boards** (`_resettle_bound_boards`, `server/api/v1/match.py`). The identity gate and the side cascade both consult the fixture and both normally run only when a *frame* arrives, so a bind/flip/unbind left the board describing the previous fixture until the feed spoke again — one frame in a busy game, forever on a paused or finished one. Gate first (it can auto-retire, which changes what the cascade decides), then `RioGameDataProvider.reorient_board(sb)`, which re-runs `_decide` against the last raw frame (`_raw_game` — `current_game` is already oriented and is the wrong input).
- **A new projector must be added to `Participants.reproject_dependents()` in the same change.** Resolve-by-copy means the copy goes stale when the *book* changes, and a projector re-projects on its own config change and at boot — not when the registry it resolves against is edited. That fan-out is the one statement of "who read the book"; every Address Book write (`Create`/`Update`/`Delete`, and **once** per batch for the two imports) calls it. Miss it and a producer's mid-broadcast name fix reaches the Address Book and nothing else until relaunch. **`Matchup` is the exception that proves the shape**: it is a *fetched* artifact with no cheap re-projection, so only its per-side tag re-resolves (`Matchup.refresh_tags`) — never re-run a network fetch from the fan-out.

> Deep dive: `.claude/skills/match-binding-lifecycle/SKILL.md` — binding schema, fixture shape, the full cascade table, identity gate, game-end detection, start.gg.

---

## Production Tab & Elements

The Production tab is a **console with three surfaces** — rack (monitor + select), stage (work on the one selected thing), quick rail (producer-pinned cards). Every element declares the same contract (registration · quick face · stage body) and all three surfaces compose from the shared row kit. **Read `.claude/skills/production-console-contract/SKILL.md` before touching `src/routes/production/`** — it is a router (surfaces · cross-cutting invariants · checklist) that names the one file under `reference/` to load for the area you're in.

- **OBS scenes are the rack's grouping axis**, and rows are **placements** (`src/routes/production/placements.js`): one row per source per scene, keyed `{element}:{board}@{scene}`. The rack lists only what is really in a scene — derivation runs source → row — and the **+** in each scene header (`addsource.jsx`) is how a source is created. There is no "phase" concept; it was removed in favour of the producer's own scene list.

- **A member that owns a source is TWO rows, and Push belongs to the slot.** The post-game callouts each have their own layout (`public/layout/postgame/{spotlight,summary}.html`) *and* sit on the Callout Stage's roster — the same shape as the hit visualizer. Their own row gets Bind/Air and no Push; their row **under** a container gets the Push. `element.flavor` is only "does it own a source"; every surface branches on `isFedPlacement(placement)`. A bespoke push button on a direct panel is the thing the source strip exists to end (the hit visualizer's "Split feed" is gone for that reason).

- **Containers are producer-built** (`src/routes/production/containers.js`): a definition in Settings (name · native size · member roster), one generic shell rendering all of them. **None ship**: a fresh install has no containers and no rules, so the catalog offers elements alone until a producer builds one. The four early pre-releases seeded (Callout Stage, Split-Screen, the Roster + Stats pair + its two rules) are removed from existing installs once (`_retire_seeded_containers`, `server/settings.py`, run-once flag because ids are slugged from names), and `Automations.settle_all` drops any feed/reason left for a container with no definition. The pre-2.0 named shells (`callout-stage.html`, `split-screen.html`) are deleted; a URL without `?container=` is not a container. **Membership lives on the container and is exclusive** — the roster *is* the relationship, so `settings.production.containers.{elementId}` and `defaultContainerFor` are gone; don't reintroduce a per-element "feed into" target. The one exception is a **container-scoped** member (`containerScoped`), which is *added* rather than moved: exclusivity answers "where does a push land", and a member with no content of its own has nothing to land — which is what makes a mirrored pair (two scoped containers, same members, one rule) buildable in the console. A member must fit the container (smaller ones center; **PRSH has no scaling system** — OBS does placement). Built from the Add picker's **+ New**, edited on the container's own stage panel.

- **A container can feed itself** (`server/automations.py` + `src/routes/production/automations.js`): a rule watches a state key and shows one of the container's members for a dwell, then returns to its **resting** occupant. The engine is a **`State.hooks` write-path hook**, so a rule's feed write rides the *same* `SetBatch` as the HUD change that triggered it — never add a polling loop for this. Precedence **manual > rule > resting** mirrored to `production.feed.reason.{container}` (modelled on `side_reason`); a producer Push suspends the rules and **clearing the feed is what hands the container back** — no separate Resume verb. Rules are only created from the **quick-add library** (templates in `automations.js`), edited on the container's stage panel (`stage/automation.jsx`); there is no free-text trigger authoring.

- OBS control runs **browser-side** (`src/context/obs.jsx`, localhost:4455) — this reaches the producer's OBS even in dual-machine setups. Don't move it server-side.
- **OBS is the control surface, not the content pipeline.** With no connection the rack swaps its scene sections for a **catalog tier** (`catalogPlacements`/`useConsoleOffline` in `placements.js`): every element and container, in the same sourceless placement shape, so the stage, previews, container rosters and feeds all keep working. The source strip's **Copy URL** shows in every state (it is not Bind's offline face — Bind just drops out), and the Add picker opens scene-less for Copy URL + **+ New** container. Don't reintroduce a surface that dies without OBS.
- **Selection, rail pins and expanded scenes are browser-local** (`usePersistentState`, `prsh.ui.production.*`) — per-producer workspace layout, never server Settings. Stored ids are **resolved at read time, never rewritten**.
- **Staging:** `src/context/staging.js` `stageOrRun` gateway — with confirm-mode on, changes stage until F9/confirm.
- **Action bus:** `POST /api/v1/action` → `v1.action` SocketIO cue to all overlays. Ephemeral one-shot cues (never stored in State), e.g. `overlay.conceal` for the OBS hide/show stutter fix. PRSH owns action names; packages own animations.
- **OBS animation contract:** animated overlays get the browser-source `shutdown` property set by PRSH (fresh load on show); a per-source "Intro" toggle (`?intro=0`) makes a source resident/no-animation instead. **Don't regress this** — see `obs.jsx` comments. **An element that animates on show belongs in BOTH halves of it** — `createRevealGate` in the mount (in-page: snap dark on hide, hold through OBS's dispatch burst, play on the PAINT clock) and `ANIMATED_LAYOUT` in `obs.jsx` (shutdown, the only cure for the OBS-native eye, which stops the source's frames before any snap-dark can be painted). The post-game callouts were in neither, so a show composited the last frame of the FINISHED graphic for a beat before its own intro started. `?intro=0` is not universal: it rests on the host's resting CSS already being its shown state, which `restingIsShown` now says out loud — a callout's resting DOM is zeroed counters and a closed backdrop, so there the param is ignored rather than honoured into a broken graphic.

---

## Layouts & Design Packages

Layouts are HTML files under `public/layout/`, enumerated by `server/api/v1/layouts.py` (type derivation from filename + group folder, `?size=`/`?team=`/`?dir=` variant expansion, `body { width/height }` size hint, `<meta name="overlay-settings">` style whitelist). Style settings are two-tier: `GLOBAL_DESIGN_KEYS` (`overlays.global.*`) and `LAYOUT_SETTINGS[layoutType]` (`overlays.{type}.{key}`), both in `src/routes/layouts/designConstants.js`.

- **Keep the `<meta>` whitelist and `LAYOUT_SETTINGS[type]` in sync.** The Production stage's Style section filters the registry down to what the whitelist names, so a key the mount honours but the whitelist omits is a setting nobody can reach. `src/routes/production/stage/eventheader.test.jsx` pins this.
- **A setting whose PART only exists at some SIZES declares `sizes` on its def.** The whitelist is per layout *type* and the scoreboard's sizes are one HTML file with one `<meta>`, so that gate structurally cannot see the difference — `settingReachesSize` (`designConstants.js`) is where it is drawn, over `sizeCodeFor`, which is the one statement of the mount's "unknown or absent `?size=` resolves to `l`" fallback. The stage passes the source's own size (`stage/index.jsx`), so a row is dropped exactly where nothing would move. Sibling of `showWhen` and `appPalette` — the other two ways a registered setting is inert on the source in front of you. The value is stored per **board**, so dropping the row never disturbs what that board's other sizes show. **The scoreboard's switch list is now almost entirely per-size**, which is what this gate is for: `showStats`/`showRoster`/`showBox` are `['l']` (Small declares none of them) and `showInning`/`showGameMode` are `['s']` (Large draws its inning inside `row-top` and its game mode inline in the linescore's meta pane, where neither costs height and so neither has anything to toggle). One registry array, ordered so each size's surviving switches read top-of-card to bottom-of-card. Removing a switch because it is pointless on the size in front of you is the bug this replaced: `showInning` and `showGameMode` were *deleted* on 2026-08-29 on Large's evidence and took Small's melded inning segment with them.
- **A boolean setting is read through `settingOn(value, fallback)` in BOTH runtimes** (`designConstants.js` + its twin in `overlay-base.js`, which is a classic script and cannot import it — pinned against one truth table in `designConstants.test.js`): a boolean is itself, `"true"`/`"false"` mean what they say, anything else leaves the default. It replaced three rules (truthiness in the overlay, `!== false` / `=== true` on the Design tab) that agree only on real booleans — and `PUT /api/v1/settings` takes a STRING, so on `"true"` the Design tab drew the text-shadow switch OFF while every overlay drew the shadow. Honouring the strings rather than rejecting them is what keeps a script or hand edit working.
- Wire every layout through **`OverlayBase.init()`** (`public/layout/lib/overlay-base.js`) — keep the HTML a thin shell over a `lib/*-mount.js`.
- **Every layout declares a sample bundle** (`init({ sample })`); `src/routes/layouts/overlay-sample.test.js` fails if one doesn't. The same bundles drive app-wide **demo mode** (`production.sample`) — see `src/routes/production/sample.jsx` for the rules that keep a fixture off a live broadcast.
- **A LINESCORE runs to regulation, and how it fills its width depends on whether it is RULED.** Two rules, both of which read as cosmetic and are not (`linescoreColumns`/`layoutBox`, both in `public/layout/lib/mount-utils.js`, called from each mount's `bindBox`). *Count*: the feeds only hand over the innings that have HAPPENED, so counting those made a live game grow its own table — four innings into a nine-inning game drew four columns. It is `linescoreColumns(played, innings_selected)` in `mount-utils.js`, the **max** of the two so extra innings stay visible, uncapped so the caller can both size its grid and window the last nine of a longer game. *Layout*: **SPREAD** — a theme names a band (`data-span-x`/`data-span-w` on `box-grid`) and however many innings there are divide it into equal cells, each column centred in its share — NOT the first and last column pinned to the edges, which stretches the inner gaps and puts the outer cells half outside a ruled frame. That is what a FRAMED table needs (Scoreboard-L draws verticals at 80/512/568, and columns that didn't fill 80..512 would leave a hole inside the box). **SLIDE** — no band declared: the PITCH never changes and the block keeps the centre the theme composed it on, because `box-total` is NESTED inside `box-grid` so the two translates compose to ±slack/2. That is what an UNRULED table needs (the Scorecard has one vertical and no frame, so stretching would make every gap in the row a function of game length — a five-inning card drawing an 80 pitch beside a dot column and an R column still sized for 40). **Which policy is not a preference; it follows from the frame.** The Scorecard used a third model until 2026-09-11 — a first→last span that rewrote each column's `x` — which both stretched the gaps AND left the dot and the R column where they were, so the two ends of the table drifted out of step with its middle. Both are pinned by `src/routes/layouts/mount-utils.test.js`; both fail silently, and a preview with the wrong data looks entirely plausible.
- Element visuals are **re-themable SVGs** bound by `data-slot`/`data-tpl` hooks via `svg-theme-engine.js`, with package resolution falling back element-by-element to `default`.
- **BROADCAST TYPE IS THREE ROLES, AND A THEME NAMES THE ROLE, NEVER A FACE** — `--font-display` (names, titles, status labels) · `--font-body` (meta, captions, prose) · `--font-mono` (scores, stats, linescores, clocks), defaulted by `rio-theme/tokens.css` to Rajdhani · Inter · Chivo Mono and repointed by the producer from the Design tab's **Typography** section (`overlays.global.displayFont`/`bodyFont`/`monoFont`, resolved by `applyTypeRoles` in `overlay-base.js`). A theme paints through a class whose NAME carries the role — `-ink`/`-lab` display, `-dim`/`-cap` body, `-num` mono **plus `tabular-nums`** — pinned per package by `tests/unit/test_design_package_svgs.py`. **A VALUE AND A SENTENCE ARE DIFFERENT ROLES** and "does it contain digits" is not the test: the mono face is the NARROWER one on every value these themes draw except a decimal (its digit is 0.600em against Inter's 0.6465em *with the tabular figures a value sets either way*, and its `%` is 0.600 against 1.0156), so the numeral face is what keeps `25.0%` off the Stat Card's auto-fit — while a sentence is mostly letters and inverts it, which is why the Scorecard's pitcher line rendered at 14px instead of 16 for as long as it was mono. **The roles survive `clearDesignSettings`** and so reach fixed-palette packages too, which is the whole feature: type is not palette, and the single `fontFamily` they replaced (migrated onto all three at overlays schema v5) was stripped on every default-package element, so it moved four layouts out of eighteen and setting it split the show in half.

> Deep dive: `.claude/skills/overlay-authoring/SKILL.md` (mount pattern, `OverlayBase` guarantees, theme contract, OBS reveal/conceal, add-a-Layout checklist) and `.claude/skills/design-package-authoring/SKILL.md` (package tiers, install rules, converting designer exports).

---

## Web Server (Port 5260)

### Networking

- Default bind: **`127.0.0.1` (loopback only).** Intentional: PRSH serves stateful APIs that mutate the live stream; binding wide by default would let anything on the LAN push fake state mid-broadcast.
- LAN access is opt-in via `server.allow_lan` (Settings). When true, binds `0.0.0.0`.
- `server/port_conflict.py` runs **synchronously before the async server** and reads `settings.json` directly. On conflict, a Tk modal offers auto-retry on the next free port, reveal-settings-folder, or quit (frozen builds).

### Static Mounts

`/assets/` (React build), `/game_assets/`, `/layout/`, `/design/`, `/branding/`. Read-only mounts are anchored to the repo root / bundle via `server/paths.py:app_root()` (CWD-independent); writable paths resolve through `user_data_dir()` (`PRSH_USER_DATA_DIR` override → frozen per-user root → `./user_data`).

**WITHOUT A `Cache-Control` HEADER A BROWSER INVENTS ONE, and that is what makes a design-package edit invisible in OBS.** Starlette sends `etag` and `last-modified` and no `cache-control`, so Chromium — hence CEF, hence every OBS Browser Source — applies the RFC 9111 §4.2.2 heuristic of 10% of the time since `Last-Modified`: a theme SVG last edited a week ago is answered from disk cache for ~16 hours **with no request reaching PRSH**. It presents as OBS alone being wrong, because a top-level NAVIGATION (the app, a pasted link) sends `Cache-Control: max-age=0` and revalidates, while the `fetch()` svg-theme-engine.js makes for `/design/{package}/{element}.svg` is an ordinary subresource and never asks. Re-adding the source doesn't help — the stale entry is keyed by the SVG's URL, not the page's. `server/http_cache.py` sends **`no-cache`** (cache it, but revalidate every use — a 304 over loopback, not a re-download) on every tree whose CONTENT changes under a URL that doesn't: `/layout/`, `/design/`, `/rio-visualizer/`, `/branding/`, `/game_assets/`. **Not `/assets/`** — Vite content-hashes it, so a new build is a new URL and revalidating is pure cost. `tests/unit/test_http_cache.py`.

API routes live in `server/api/v1/` — decorate with `@method` and register in `server/api/__init__.py`.

### SocketIO Events

- `v1.state.set` / `v1.state.set_batch` (+ `unset` variants) — state updates (server → clients). **Every consumer must handle both** — overlays get this for free via `OverlayBase.init`.
- **A `set_batch` frame has TWO lists: `items` and `augmented`.** `items` is what the caller wrote; `augmented` is what a write-path hook (`State._augment` — today the container automation engine) decided *off* that write, riding the same frame so a consequence never lands a frame after its trigger. The split exists because a client suppresses the echo of its **own** frame by `sid`, and a hook's entries are not its write — they are the server's answer to it. Mixed into `items` they were dropped by the one client that needed them most: a producer's Push got `production.feed.reason.{container} = manual` back and never saw it, so their console kept reporting the container as *resting* — rules still running — while every other browser had it right. **Every consumer must read both lists** (`src/context/socket.jsx`, `public/layout/lib/overlay-base.js`).
- `v1.settings.set` — settings updates (bidirectional).
- `v1.state.get` — client requests full state on connect.
- `v1.action` — ephemeral action cues (see action bus).

---

## Tournament Integration (start.gg)

Start.gg GraphQL via `StartGGProvider`, loaded by event URL/slug on the Competition tab. **Match-first:** a set loads into a `match.{M}` and the producer binds that match to a board. Both the API route and the bracket view's "Load to Match" go through `apply_startgg_set`. Unseeded phases produce string preview ids (`preview_…`) — set ids are `int | str`.

There is **no direct set→score path**. (Challonge was fully removed in July 2026; don't reintroduce provider branching.)

**Every start.gg write into `tournamentInfo` goes through `auto_fill_entries`** (`server/startgg/provider.py`), which is the one statement of the auto-fill rule: overwrite a field only while it is still empty or still equal to what we last filled it with (recorded per field in `tournamentInfo._auto`), so a producer's hand edit survives a reload and re-blanking hands the field back. Two callers, and the second is why it is a function — `_load_event` fills five fields once per *event*, while `apply_startgg_set` fills `phase` once per *set* and used to write unconditionally, destroying a typed Competition Phase on every fixture load. `skip_blank=True` is the per-set difference: a set that reports no phase is saying nothing, where an event with no address is saying it has none.

**The Competition tab holds facts about the event; a value only ONE overlay reads is that element's own setting.** The Event Header's banner line was `tournamentInfo.message` and is now the message field's own text on that element's bands, authored on its Production stage panel. Ask which surfaces read a value before deciding where it is authored.

**The Competition tab is ONE page about one loaded event** (`src/routes/competition/competition.jsx`): the loader, the competition form in a fixed 40rem left column (`tournament_info/tournament_info.jsx` — the form and nothing else), and a right column holding whichever *list* the producer wants — **Entrants** (`competition/entrants.jsx`, setup-time: map the field into the Address Book) or **Sets** (`bracket/bracket.jsx`, run-time: browse the bracket and Load to Match). It used to be two pages behind a segmented control, which asked a producer to choose between the event's facts and the event's sets and bundled the form in with the entrants table — so the form's own width was a function of whether a tournament had been loaded. **The switch chooses a list, never a page**; the form stays on screen for both. Keep the two lists mounted (switching must not refetch), and say "nothing loaded" once, on the page, not once per list.

**The entrants list carries only what a producer acts on** — seed, tag, Address Book state, Rio ID — plus a census of what is still unmapped. A start.gg profile's full name, pronouns, country and state are the **Address Book's** fields; the import copies them there (`sg_to_display`, `server/participants.py`) whether or not this page draws them, so a column here is a read-only duplicate crowding out the two fields the page exists to change. **A sponsor prefix is never displayed anywhere** — not on an entrant, not on a set — which is why `entrant_name` in `server/startgg/parsers.py` answers with the participant's bare `gamerTag` rather than start.gg's `"AAA | Alice"` entrant name (the cached-bracket path always did, so the two disagreed); it falls back to the entrant name for a team or a preview set, where there is no single tag.

---

## Platform-Specific Behavior

- **macOS** — system-tray icon (`tray.py`, pystray) holds the process; no visible window.
- **Windows** — `win_window.py` opens a Tk taskbar window — the only clean way for end users to close the app on Windows.
- **Both frozen only** — `port_conflict.py` dialog if the configured port is taken. Source checkouts skip all of this and run headless.
- **Controller overlay (gc-overlay)** runs on **every platform** and is bundled via the `gc-overlay/` submodule. It had four platform gates until 2026-08-28; gc-overlay 1.1.0 carries two peer Dolphin transports (MemoryWatcher on macOS/Linux, a process-memory poll on Windows/Linux — the constraint is inverted between them), so **presence, not platform, is what gates it**. Don't reintroduce a gate. **Everything PRSH asks of the reader it asks in a QUERY STRING** — port (1-indexed there, 0-indexed in the HUD), `bg=transparent`, and the chrome switches that keep gc-overlay's gear/port-label/status text off the broadcast — so a dropped param is silent in both codebases and wrong only on air; the broadcast string is one constant (`GC_CHROME`, `lib/controller-mount.js`), pinned by `src/routes/layouts/controller-mount.test.js`. Its native size is **512×180** as of gc-overlay 1.2.0 (was 512×256) and lives in four runtimes. Its three APPEARANCE settings are ordinary per-element ones (`overlays.controller.*`, on the element's stage panel); an app-wide copy on the Connections tab with three-state pins over it was tried and removed, because a pin beats a global and so touching one element silently cut it off from the control meant to drive it. The idle fill is an **opacity** with a slider — a 0–1 number field was given `10` (ten percent) and drew solid. See `.claude/skills/controller-overlay/SKILL.md`.

### MSB Image Assets

PRSH does not ship MSB images (Nintendo IP). Users provide an asset pack under `user_data/game_assets/msb/{characterIcons,teamLogos,gameIcons}`. `server/api/v1/assets.py` validates against pyrio's canonical filename lists and surfaces missing files in Settings and the Welcome card. Custom location: `settings.assets.msb_path`.

### HUD File Default Paths

- macOS: `~/Library/Application Support/Project Rio/HudFiles/decoded.hud.json`
- Windows: `%APPDATA%\Project Rio\HudFiles\decoded.hud.json`
- Override: Connections → Project Rio — HUD file

### Announcements & Logs

`server/announcements.py` merges a GitHub release check with `announcements.json` on the repo's `announcements` branch; per-user dismissals in `settings.announcements.dismissed_ids`. The in-app log viewer (`LogsViewer.jsx` + `server/api/v1/logs.py`) tails rotated logs by filename with path-traversal guards.

---

## Build Notes

`npm run dev` sets `TSH_DEV=1`, which enables CORS for the Vite origin. `scripts/freeze-version.py` bakes the version from the git tag during `prebuild`; `pyinstaller PRSH.spec` then produces `dist/PRSH.app` / `dist/PRSH/PRSH.exe`, and `installer/PRSH.iss` produces `PRSH-Setup.exe` on Windows.

**A build that is not LAUNCHED is not tested.** `build-release.yml` runs `PRSH --selftest` (`_selftest` in `main.py`) on every artifact: the module-level import graph, the platform modules imported lazily inside the frozen branch, and the data paths read off disk — the two things importing `main.py` cannot reach, both silent when missing — then exits. It exists because prerelease 12 shipped unlaunchable on every platform at once: `caio`/`aiofile` read their own version via `importlib.metadata` at import, PyInstaller bundles modules and not distribution metadata, and the process died before PRSH's first line — no log, no server, only PyInstaller's dialog. Nothing in PRSH changed; `pip install .` resolved the chain unpinned. `PRSH.spec` now collects that chain's metadata recursively, so the next link to grow the lookup is already covered.

### Git: submodules and `gh`

- **Push a submodule BEFORE the PRSH commit that pins it.** A pin is a bare SHA, resolvable on the machine that made it and nowhere else until the submodule is pushed — so pushing PRSH first publishes a pointer only that machine can follow, and CI dies in `git submodule update` with `upload-pack: not our ref`, naming neither the submodule nor the cause. `scripts/check-submodule-pins.py` runs from `.githooks/pre-push` and blocks exactly this (enable once per clone: `git config core.hooksPath .githooks`; override with `git push --no-verify`).
- **PRSH is a GitHub fork of TournamentStreamHelper, and `gh` resolves a fork to its parent.** Left unset, `gh run list` returns *nothing* and `gh run rerun` 404s against the upstream repo — quietly, as an empty result rather than an error. Fixed here via `gh repo set-default matt-gree/ProjectRioStreamHelper` (`remote.origin.gh-resolved`); a fresh clone needs it again. The `upstream` remote was removed — we never pull from TSH, and it carried a live push URL to a repo we don't own.

## Testing

Both suites run in CI and **both must stay green**. Design doc: [TESTING.md](TESTING.md).

- `tests/conftest.py` autouse fixtures (`mock_socket`, `isolate_user_data`, `reset_singletons`) redirect State/Settings persistence to `tmp_path` and reset singletons — reuse them; **never let a test touch real `user_data/`**.
- Integration tests run in-process via `fastapi.testclient.TestClient` over `router_v1` (no uvicorn/socketio boot).
- When changing behavior a test encodes (e.g. the side cascade), update the test *in the same change* — code, tests, and this file must never disagree.
- Manual smoke (still worth doing for UI/overlay changes): `npm run dev`, check console, load a layout in OBS/browser and hard-refresh it.

> Deep dive: `.claude/skills/run-and-verify/SKILL.md` (fixture contract, per-area verification recipes, isolation caveats)

### Clearing Cached State

If the app fails to launch due to corrupt `user_data/state.json`: `echo '{}' > user_data/state.json`. In-app: Settings → Help & recovery → Reset boards and matches (`POST /scoreboards/reset`).

---

## Key Files for Common Tasks

| Task | Files |
|------|-------|
| Change HUD game parsing | `server/rio/provider.py`, `server/rio/hud_watcher.py` |
| Side cascade / orientation | `server/rio/provider.py` (`_decide`, `_preserve_player_sides`, and `pin_swap` — the pin layer, module-level because `rio/game_pool.py` and `api/v1/game_pool.py` import it too) + `tests/unit/rio/test_side_preservation.py`. The pin's *data* is `prefs.side` on a participant, so a pin test seeds the address book (`pin_player` fixture), never a setting |
| Binding model (pool/playback/transport) | `server/bindings.py`, `server/rio/rotation.py`, `server/api/v1/scoreboards.py` |
| Match lifecycle / series / start.gg sets | `server/match.py`, `server/api/v1/match.py` (`bind_board` owns board↔match), `server/rio/game_end.py` |
| **CREDITING A SERIES GAME** (and the once-per-game guard that must be durable) | `Match.award_game` + `PostGame._promote_match` + `tests/unit/test_postgame_series_credit.py`. **THE STAGE HOP AND THE CREDIT ARE TWO THINGS.** `_promote_match` nested the second inside `if stage != 'post'`, which reads as a once-per-game guard and is a once-per-**MATCH** one: game 2 of a Bo3 arrives with the match already at `post`, so it was skipped in silence — **a series could never get past 1–0**, never set `decided`, and its board sat on `between games` forever with no take offered. The same skip swallowed every re-capture after a first one that could not resolve a winner (a **quit game reports no `winnerSide`**, credits nothing correctly, and moved the stage anyway), which is how a **Bo1** ends a night 0–0 with three captured games behind it. The real once-per-game guard is `award_game`'s `game_id` dedup — now **durable**, on `match.{m}.credited` (`{game id: side}`), because the class-level set it replaced was empty on boot and *that* is what the stage guard was standing in for. It rides `match.{m}`, so it is unset with the fixture and needs no teardown of its own. Two reporters exist for one finished game (the HUD capture and the API `GameEndWatcher` on another board bound to the same match), and both pass an id. **The dedup is not the only guard**: `is_complete` is checked after it, because "have I already counted THIS game" and "is there a game left to count" are different questions and only the second one catches the game after a finished fixture — see the series-cap rule above |
| Fixture authoring UI (sides, format, series, flip/decide/reopen, board bind) | `src/routes/production/desks/match.jsx` — the only place. **`NightLead` is the desk's "what now" strip and is deliberately the SAME SHAPE as the board desk's turnover bar** (one sentence naming the state, one filled press ending it): the console should have exactly one thing that looks like this, and a producer who learnt it on a board should not have to learn it again here. It renders only where the desk genuinely owes a press — `idle` = nothing waiting for a board AND nothing coming (every fixture decided, or none authored), which is the cold start; `none waiting` is deliberately not idle, because those fixtures are on boards or held back per fixture and `StageControl` explains that where the fixture is. The footer `New match` stands down while the strip carries the verb, or the panel offers one press twice; with SEVERAL running orders the strip carries no `New match` at all and points at the `+` that can say which order — the same trap `QueueHeading` exists to avoid. **CLEARING COMES BEFORE ADDING WHEN THERE IS SOMETHING TO CLEAR**: a finished night's loud button was `New match`, which is a forward press stepping straight over the obvious question and whose outcome is tonight's fixture buried under last night's — so `ClearPlayed` is the strip's `default` and `New match` drops to `ghost`. Clearing lands on the EMPTY state, where `New match` is the primary and the only thing on the strip, so the two steps chain and each has exactly one obvious press. What it replaced was a paragraph explaining what a match IS ("participants, captains, bracket phase, mode and format — then bind it to a board…") where a button belonged. The Match tab is **deleted** (`MatchPanel`, `ScoreControls`, `PoolBrowser`, `src/routes/scoreboard_manager/`); `/scoreboard` renders the console for old bookmarks |
| Match queues (membership, order, "up next" per board) | `server/schedule.py` (`queues`, `append`/`move`/`remove`, `next_up(qid)`, `next_up_for_board`, `queue_for_board`, `ensure_migrated`, `reproject`, and `not_waiting_reason` — the one statement of eligibility; **`_commit` owns all change detection**, so no verb early-outs past the projection), `server/api/v1/schedule.py` (queue CRUD + per-id verbs; `move` takes no queue id on purpose), `take_next_match` in `server/api/v1/match.py` (board's order + `?queue=` override); client `src/context/schedule.js` + `src/routes/production/queue.js` (`useQueues`, `useBoardQueueId`, `useNextUp(sb)`, `useNextInOrder(qid)`, `useWaitingReason`), authored on `desks/match.jsx` (`QueueHeading`, `MembershipControl`, `NewQueueButton`), assigned on `desks/board.jsx` (`BoardQueueRow`), taken on `desks/board.jsx` + `quickface.jsx`. The Upcoming Schedule stage panel (`stage/schedule.jsx`) authors none of it — heading, per-match time, and a readout of which of the order the board is actually drawing |
| **THE UPCOMING SCHEDULE DRAWS A SELECTION OF THE ORDER, NOT THE ORDER** | `scheduleRows` in `public/layout/lib/schedule-mount.js` (+ `src/routes/layouts/schedule-mount.test.js`), drawn by `public/design/*/schedule.svg`, settings in `LAYOUT_SETTINGS.schedule`. `schedule.queue` is the union of every running order and **nothing ever takes a fixture out of it**, so a board that drew it would grow all night and keep a decided match on an "Upcoming" graphic forever. Two settings follow from that one fact: **Decided Matches** (off by default) and **Rows Shown** (the rest fold into a "+N more" line). The cap is the CANVAS, not a taste — the card is `data-compact-h + rows × data-h` and centres on 1080. Both numbers are the THEME's, so a package can change its row height without a code change. **The console imports `scheduleRows` rather than restating it** (`stage/schedule.jsx`, the `resolveSpotlight` shape): the panel lists every queued match, because a time has to be editable on one the board is not drawing, and marks which are on air — a readout that numbered the queue would claim the board shows nine when it shows six, in exactly the state the settings exist for. **THE STATE CUE IS ONE COLOUR AT THREE STRENGTHS**: live / upcoming / played is the rail's `fill-opacity` over the theme's accent, never three hexes, so the live row is the only thing on the card at full accent. It was a hand-rolled DOM card list until 2026-09-13, which is why its `<meta>` whitelist advertised `accentColor`, `cardBg` and `textColor` while the mount called neither `applyDesignSettings` nor `clearDesignSettings` — three knobs and three type roles that wrote settings nothing read, on an element that looked plausibly on-theme because its CSS reached for the Rio TOKEN vars (`--band`/`--ink`) directly |
| Removing a played match (and when one is removed at all) | **Nothing removes a match automatically** — no age cutoff, no count cap, no session scope; `match.{M}` lives in `state.json` and survives every restart, and ids climb with `max(ids) + 1` rather than being re-used the way board ids are. That is right (auto-deleting is guesswork about what a producer still wants on screen, and is the same ceremony-nobody-is-there-for as the rejected `End session` boundary), but for a long time the only two verbs were the trash on ONE card and `POST /scoreboards/reset`, which also resets every binding and playback mode and so is never reached for to tidy. Removal is now TWO verbs at two scales, and nothing hides a row. **Per fixture**, on its own bar: `Clear`, whose PROMINENCE TRACKS WHETHER IT IS THE EXPECTED NEXT STEP — a decided row gets a labelled `secondary` button, every other row keeps the 14px trash glyph, because deleting a fixture that has not been played is a rare correction rather than the shape of the night. It does not MOVE between the two faces; a control that relocates when its record changes state is its own confusion. **In bulk**, `ClearPlayed` on the night strip, which is the panel's one `default` press and covers every match (that strip only appears when every match IS decided, so its scope needs no qualifier). Row clears stay `secondary` under it: eight filled red rows would be a wall of alarm that teaches a producer to stop reading them. **A PLAYED MATCH IS AN ORDINARY ROW** — the leading decided run used to collapse behind a `2 played ›` disclosure (`PlayedFold`, deleted 2026-09-13), which made the thing a producer most often wants to act on at the end of a night the one thing they had to open a fold to reach, and gave the desk two row shapes for one kind of record; the result badge already says a fixture is finished. What the fold was really solving is that a night accumulates and nothing removed a fixture, which is what the clears answer. **`Clear` is the one word this desk uses for removing a fixture** — the confirm popovers say "Deletes the fixture" in the BODY, so permanence stays honest while the verb stays single. **It names the board it will blank**: a decided fixture STAYS BOUND (the whole reason the fixture slot has a `done` branch), so a clear can unbind a board that is on air — excluding bound matches instead is worse, because the last match of a night is almost always still on its board and the producer would be left with exactly the one they wanted gone. Sequential `deleteMatch` by EXPLICIT id, never a list re-derived between calls: every delete re-projects the running orders |
| Deleting matches outside `delete_match` | **Call `Schedule.reproject()`.** `queues()` prunes dead ids on *read* only, so a path that unsets `match.{M}` directly (`POST /scoreboards/reset`) leaves the stored `schedule.queues`/`schedule.queue` listing fixtures that no longer exist — and the console subject row counts the projection. `ensure_migrated` re-projects at boot for the same reason |
| Match lifecycle (`stage`, and why a fixture isn't up next) | `StageControl` in `src/routes/production/desks/match.jsx` (momentary `PUT /match/{m}`), `Schedule.not_waiting_reason` + `useWaitingReason`, `tests/unit/api/test_schedule_waiting.py`. Server writers only move forward: `Match.note_live`, `server/postgame.py`, `server/rio/provider.py`. **A DECIDED FIXTURE DOES NOT DRAW THE BADGE AT ALL.** The control exists to answer "why is this not coming up?"; on a finished fixture the answer is "because it is finished", which the emerald `Side N wins` badge beside it has already given in the producer's words rather than the state key's — so `POST` next to `SIDE 1 WINS` was two chips for one fact, one of them in app vocabulary. It was also actively misleading: the popover's one verb is `Ready next game`, which on a decided match writes `stage = draft` and changes nothing about whether it is offered, so it looked like the way out of a state it cannot leave. `Reopen` is the only move a finished fixture has and is now the only one shown |
| A board's games (pool, playback, rotation transport, game search) | `src/routes/production/games.jsx` (`usePlaybackMode` + `PlaybackModeControl` = the choice, `GamesSection` = the chosen half's surface; the one dialog is the rotating pool's member list), rendered by `desks/board.jsx` as its own `Games` region; server side `server/bindings.py`, `server/rio/rotation.py`, `server/api/v1/rotation.py`. **RIO'S OWN CAP IS 50 AND THE LIMIT FIELD SAYS SO** — `DEFAULT_LIMIT`, the one statement of it, seeding `EMPTY_FILTER`, the field's fallback and the completed search's query (which carried its own, different `?? 100`). A search that names no `limit_games` still returns the newest 50, so the field's `All` placeholder described the most limited state on offer as unlimited, and clearing the box to widen a pool narrowed it back to the default with nothing saying so. **Every number on this surface is a kit `NumberField`**, never the bare `NumberInput`: the two intervals were controlled straight off the stored value with a `val || 30` at the call site, which turned the keystroke that CLEARS the box into a write of the default and then refilled the box with it — clearing 30 to type 120 gave `30120`, and an empty field was unreachable. See the number-field rules in the console skill's `row-kit.md`: keystrokes local, `clearable` says what an empty box MEANS, and `min`/`max` clamp on blur and never on the timer. **THE REGION'S RULE CARRIES THE CONTROL, NEVER A SENTENCE ABOUT IT.** `playbackLine` wrote that rule's subject until 2026-09-14 — `Rotating — nothing in its pool yet`, `One game, pinned — following it live` — and every clause of it was drawn again within a few rows: the MODE by the segmented directly beneath it, the POOL COUNT by the rotator's own status line (the only surface that can also say *why* a pool is empty), `(paused)` by the Start button, and `following it live` by the refresh countdown beside it — a caption on a clock, telling you what the clock is doing. The segmented moved onto the rule in its place (`KitColumn subject`, the slot the feed's badge and mode picker ride one region up) and stopped being `fullWidth`, which on a full-width desk had drawn a two-option control as a ~1100px bar. **The VALUE is owned by the desk**, called once and passed to both halves: the hook carries a local echo of a settings round-trip, so two calls would be two echoes free to disagree about which mode the board is in. Transport and playback stay two axes a region apart — the badge is DERIVED, the segmented CHOSEN — and `boardTypeTag` is that pair compressed to a rack row, which is all that is left of the sentence |
| Game-mode vocabulary (pickers, the pool's mode filter) | `src/routes/production/gamemodes.js` (`useGameModes` = active + ended tiers, `withHeldModes` = plus whatever a surface already holds) — the one source for every mode picker; server side `GET /rio/game-modes[?scope=all]` (`server/api/v1/stats.py`) over `stats_api.fetch_game_modes` (active, in-memory) / `fetch_all_game_modes` (the ~200-mode catalogue, pyrio's disk cache). **The active list is not the vocabulary**: a board's mode comes from the game it carries, so anything that must *say* or *match* a mode takes the catalogue |
| Add/modify state keys | `server/state.py`, `src/context/store.jsx` |
| **WHERE A BOARD'S GAME IS UP TO** (and why a board that boots is not a feed) | `server/boards.py` (`lifecycle_of` / `board_lifecycle` / `is_stale`) + `src/routes/production/boards.js` (`boardLifecycle` / `isStaleBoard`), pinned against ONE shared case table (`tests/fixtures/board_lifecycle.json`) read by both suites — a behaviour, not a literal, so parity is cases rather than parsing one language from the other. The server wrote `game_over`/`game_completed`/`live_following` and never read them back together, so server-side code that needed "is a feed driving this board" asked `game_id` instead, which a finished game satisfies forever. **The fifth state is `restored`**: the game came off disk at boot, not from a feed in this process — `State.Load` restores yesterday's score and the provider then re-reads `decoded.hud.json`, which still holds yesterday's last frame, so the app opened the next day fully populated with nothing anywhere saying the data was 18 hours old. It is **not a session marker and has no verb**: `apply_parsed_game_to_state` writes it False in the per-frame batch, so the flag **decays** — the next game's first frame retires it with no producer action, and an app restart mid-broadcast shows it for one frame and goes back to LIVE on its own. That decay is why a `End session` boundary was rejected: a ceremony fires at a moment the producer has already left. `boards.mark_restored()` runs in the lifespan **after** `RioGameDataProvider.Start()` and before the pools — the boot HUD read goes through the ordinary per-frame batch, so marking first would be undone by it, and marking after is what lets an archive read be treated as an archive with no argument threaded through every apply path |
| **CLEARING A BOARD'S GAME** (the inverse of the two appliers) | `clear_game_entries` + `release_and_clear_game` (`server/rio/provider.py`, beside the appliers they invert), `POST /scoreboards/{sb}/clear-game`, called by `resetGame` in `desks/board.jsx`. **The re-projection is the point**: the Match projector jointly owns `player.{T}.*` and runs only on a bind or a fixture mutation, so a clear built from plain state writes in the browser blanked the bound fixture's names with nothing left to restore them — the slot read `M2 · Alice vs Bob` while the scoreboard drew nobody, and the workaround was knowing to clear BEFORE binding and never after. Blank first, re-project second: a board with no `game_id` is one the projector writes its full key set to, picks included. **The list is derived, never re-listed** — resurface targets come from `RESURFACE_MAP`, because the browser's copy and `test_state_completeness`'s copy had drifted on `player.{T}.name` while BOTH missed the six address-book fields, so a clear left the previous player's pronouns and socials on air; and the clear now inverts the **completed** applier too (`innings_played`, which sizes the linescore, `date_time_*`, which `meta-date` draws, and the winner/loser/elo eight), which it had never covered at all. Pinned as a set comparison against each applier, so a key added to either fails the moment it is added. **`release_match` IS THE ANSWER TO "I CLEARED IT AND IT CAME BACK"**, and it is the other half of the re-projection above. Keeping the binding is right while the fixture still has a game to give — a Bo3 between games must keep its match on the board — but over a DECIDED one it produced the console's most baffling outcome: the clear blanked the board and the projector immediately repainted the fixture's two names onto it, so the scoreboard went back to drawing last night's finished matchup at 0-0. The producer pressed the button that empties a board and the board did not empty; the way out was two presses on two different rows (clear here, unlink in the fixture slot) with nothing saying so. The flag is set from the ONE condition that answers it — `bound && decided`, i.e. the fixture has no further game to put here — and released FIRST, through `_unbind_board`, which is the one statement of the unbind rule (drop the keys, blank what the projector owns, then RE-SETTLE, or the board keeps explaining its orientation by a `side_reason` layer that is no longer there); the clear then finds no match to re-project, which is the intent. **TWO NAMES, BECAUSE THEY ARE TWO DIFFERENT ACTS** — `Clear & unbind M{id}` vs `Clear game`, because one label that sometimes unbinds leaves a producer unable to tell from the button what their board will be holding afterwards, which is the only thing they are asking it. The releasing name **says the fixture out loud** for that same reason: it was `Clear board`, whose entire difference from `Clear game` was one word for a container you have to already know contains the match. The board desk's own escape hatch never releases: it is the every-other-time verb, and a live board's binding must survive a hand correction. **It clears the post-game capture too** (`PostGame.clear`, both modes): a capture is the cleared game's receipt, and kept it went on driving the Game Summary and Spotlight and read `CAPTURED` beside a board that no longer held its game — the stat file is still on disk, so the region's file picker is the way back. **And on a HUD board the clear SURVIVES A RESTART**: `release_feed` hands back the released frame's `game_id:event_num`, persisted at `rio_hud.released_frame` in the same batch, so the boot read of `decoded.hud.json` (which holds that game's last frame forever) does not put it back — `_on_hud_game_update_impl` holds a matching frame, a DIFFERENT frame (Rio writing again) ends the hold, and `ReloadHudPath` (Re-read HUD, a new path) forgets it first. `_feed_released` alone was memory. It does not touch the pool. **The STATS TAG it does clear** (`clear_stats_tag`, `server/bindings.py`): the mode is the GAME's, not the board's — `stats_tag` is the feed's answer unless a producer overrode it — so leaving the last game's season on an emptied board left the one control on the game rule still describing the game just taken off it. **Both keys**, or it makes things worse: `sync_stats_tag` stands down while `stats_tag_manual` is set, so clearing the value alone would blank the mode and then stop the next game from ever filling it back in, trading a stale name for a permanent blank — a Bo1 decides the moment it is captured, so the release fires at exactly the moment its Game Summary is most likely to be on air |
| A rule that lives BETWEEN models (projector vs feed, container feed vs roster, `schedule.queue` vs its orders) | `server/invariants.py` — the one written statement of the cross-subsystem rules, run from three places off that one list: `tests/integration/test_invariants.py`, `GET /api/v1/invariants`, and `scripts/prsh-agent.py doctor --assert`. Every subsystem here is unit-tested and correct alone; **these are the bugs no module's tests can see**, so a new rule that spans two models goes here rather than into one of their suites |
| Driving the app as a producer (boot an isolated instance, reach an interesting state, read it back) | `scripts/prsh-agent.py` (`up`/`down`, `scenario`, `state <prefix>`, `hud --set`, `doctor`) + the `drive-the-app` skill, which also carries the UI map and the false positives an isolated instance manufactures |
| Add API endpoints | `server/api/v1/` (decorate with `@method`), register in `server/api/__init__.py` |
| Settings schema | `server/settings.py` (defaults + migrations); authored in two places, split by **what a setting points at**: `src/routes/connections/` for anything aimed OUTSIDE PRSH (HUD file, MSB pack, gc-overlay, OBS, LAN bind) and `src/components/SettingsModal.jsx` for preferences and the escape hatches. See the Connections row below before adding a control to either. **Every modal row is one `SettingRow`** (label + at most one line of hint on the left, the one control on the right, `htmlFor` making the words the control's name), grouped General · Production · Output · Help & recovery by what the setting is FOR — three row layouts made the eye find the control again on every row. **There is no theme setting: the app is dark-only.** The palette was only ever the night one, so "Light" just dropped the `dark` class and switched off every `dark:` variant over dark tokens; the class is on `<html>` in `index.html` AND added by `main.jsx`, because the server serves whichever `index.html` it finds (a stale `dist/` copy in dev). **CHANGING A DEFAULT REACHES NEW INSTALLS ONLY** unless you adopt it forward: `_deep_merge` writes the whole settings dict back, so a default is persisted the moment anything else is saved and is from then on indistinguishable from a producer's choice. A stored value still EQUAL TO THE OLD DEFAULT is therefore migrated to the new one (`_adopt_opaque_stroke`, which took the font border from 90% to opaque) — across EVERY `overlays` namespace, recursively, because a per-element or per-board pin is seeded from the global and holds the old default for the same reason. The cost, stated in its docstring, is a producer who deliberately chose exactly the old value |
| Per-element style overrides (a global design key pinned for one element) | `src/routes/production/stage/overlay-settings.jsx` (`ElementStyleOverrides`), `OVERRIDABLE_GLOBAL_KEYS` / `OVERRIDE_CAPABLE_TYPES` / `overrideReaches` in `src/routes/layouts/designConstants.js`, the layout's own `<meta>` read back by `src/routes/layouts/layoutWhitelist.js`. **Three gates, none implying another**: the registry offers the key, the layout declares it, and overlay-base actually reads it back on that type. A key failing any gate is **never offered** — an option that silently does nothing costs more than a missing one. The section is an **exception list, not a settings panel**: nothing is on an element until the producer adds it from the `AddOverride` picker, which lists only the unpinned keys that pass all three, and one **×** per row is the only way off (`ColorRow`'s `hideReset` and the dropped font Reset exist so there is exactly one). **Empty is therefore its usual state, and it wears no HEADING there** — a caps "Style overrides" over one ghost button reading "Add style override" is a second name for the only thing under it; the divider stays, because that is what says the button starts a new group rather than belonging to the style settings above. **Its footer row also carries a GUEST** (`leading`): the stage's intro control (`stage/intro.jsx`), which was a `KIT_SECTION` headed "On show" holding one `ToggleRow` directly above this one — two panel-level, set-once controls with most of a panel's width dead beside each. Three separate faults, worth keeping apart because each recurs on its own: a **section of one** (the heading is a second name for its only member); a **switch for a preference** (`bg-primary` spends the console's on-air colour on a set-once knob — and a lone `ToggleChip` is worse, since fill-as-state needs the filled siblings of a STRIP to read as state at all, so a segmented pair that NAMES both states is what survives with nothing beside it); and a **label column with no column** (`KIT_LABEL` is 128px so a panel's rows line up — right for a column, and on a lone row it just strands the label from its control). Pass the guest as `introTypeFor(element) ? … : null`, never the component: `IntroRow` renders null on its own, but the element is always truthy, and the spacer that pushes Add right is gated on the guest existing. Adding **pins at the value the element already shows** (`seedValue`) so it hands over a knob without turning one — every def must seed non-null or the write would mean *unpinned* and the row would never appear, which is why `finalBadgeColor` (no global of its own; the badge is `fill:var(--accent)`) carries `seedFrom`. Membership counts a **staged** pin too, or adding one under confirm mode empties the panel it was added to. **The three FONT roles are gated differently from the palette, because type is not palette**: they survive `clearDesignSettings`, so a font pin reaches a full-art theme too, and what makes one dead is the theme setting nothing in that role — offered only where `drawnTypeRoles` (`designPackage.js`, over the server's per-file `typeRoles` scan in `server/design_packages.py`) says the active theme draws it (default's Commentary has no numerals). Gated on the palette tier, every font was locked on every themed element under `default`. An element no theme draws answers from its whitelist alone, which is why Player Name and the Event Header declare `displayFont` and nothing else. Under a full-art package existing palette pins (and, under any package, a pinned role the theme does not draw) have their VALUE CONTROL disabled — not dropped, and **never the ×**: a pin you cannot see is one you cannot remove, and greying out the remove button defeated the reason the row is kept (removing is also the only act that still means something there, since the pin is stored and revives on the next package swap) — while the picker closes, explained by a **`title` on the hoverable wrapper** rather than a paragraph above the section — a disabled `Button` is `disabled:pointer-events-none`, so a tooltip sitting on the control itself is unreachable exactly when it is the only explanation there is. Contrast the element's own `appPalette` settings, which `useLiveDefs` drops. **A colour and the size of what it paints are ONE override** (`colorKey` names the partner, `partner: true` keeps it out of the picker): Font Border + its colour, and the text shadow's blur + `textShadowColor` (which became overridable for this). One option adds both, one × removes both — a pair operation, or the × leaves a colour pinned that nothing draws and nothing can reach — and a row is ON if EITHER half is pinned, so settings written before the pairing (a colour with no size) are not stranded |
| **THE MATCHUP SUMMARY'S SPACING IS ARITHMETIC, NOT TASTE** | The score PLATE + `pinBesideText` / `applyTextPins` (`public/layout/lib/mount-utils.js`, `src/routes/layouts/mount-utils.test.js`), drawn by `public/design/*/matchup.svg`. Measured on a real head-to-head with no set bound, the strip held **five pools of dead air**: the name ended at 705 and the win count began at 849, so **144 units** of open band sat between a name and the number it labelled (129 on the mirror side); the name block reserved 88 units for a captain portrait that exists only once a match is bound; and one static caption said "LAST 5 MEETINGS" on the left while the all-time count floated alone above the score. **A PLATE turns a hole into a margin** — the counts sit in one, so the air either side became the border of an object the eye can see and a name of any length varies that margin instead of opening a gap (28 units now), while each count is centred in its half so a two-digit total grows 24 each way rather than dragging one edge out by 49. **A PORTRAIT BESIDE A NAME HAS NO AUTHORABLE `x`**, because the name's width is the data: fixed outside an inward-anchored name, `Joan` (82 units) left her captain icon floating 170 from it while `MattGree` (153) sat 95 away; fixed *inside*, next to the score, only moves the hole to the far side of the name and holds 108 units of it open in the common state. So the theme declares `data-pin-before`/`data-pin-after` + `data-pin-gap` and the mount places it from the MEASURED edge — **after `refitText`**, since the auto-fit changes the width being measured, which is why the two travel as one `fitAndPin`. Like every `data-*` a mount reads, the trio is in `_MODIFIERS` (`server/theme_compiler.py`) or the Figma round trip returns a theme where the portrait silently reverts to a fixed `x` that is wrong at half the name lengths. Two more: **one caption, carrying data** (the cards each state a full date now, so the static one had nothing left to tell anyone), and **a card's date carries its YEAR** (`game{i}-date-full`) because a head-to-head reaches back seasons and a bare `SEP 8` on the fifth card dates it to nothing. The marquee stays centred on 960 even with the right third empty: it would otherwise jump the moment a set loaded. **A CARD'S FOOTER IS ONE LINE**: the mode and the stadium were two dim 19px lines stacked in the same face, colour and weight, so they read as one indistinguishable grey block rather than as two facts, and together they were the tallest thing on the card after the rows. The mode is the competition and earns its place; the park is flavour a history card does not need (Slice26 dropped it first, and it is the producer's call). `game{i}-stadium` stays a slot the MOUNT binds, so a theme that wants it only has to draw it — dropping a fact from a shipped theme is not dropping it from the contract. What remains matches the DATE it brackets (17px, not 19), so the two captions frame the rows instead of competing with them, and the line they gave up went to the rows: 48 units and snug, 56 now |
| **WHICH PLAYER WON A MATCHUP CARD** | `rowOutcome` in `public/layout/lib/mount-utils.js` (+ `src/routes/layouts/matchup-mount.test.js`), drawn by `public/design/*/matchup.svg` — **and by the Results Ticker's cards**, which is why it moved out of `matchup-mount.js` on 2026-09-15: one kind of card owes one answer, and the ticker was about to be the second copy of it. **An opacity is not an answer to "who won"** — the head-to-head band's only outcome cue was a 0.45 dim on the losing SCORE, and dimmed white on a near-black card is not a cue at broadcast size. The worse half is that the card it sat on **named neither player**: two team logos and two numbers, so reading the dim right still only told you which *number* lost. The logos are no substitute either — they are the CAPTAINS' teams, so one player is Mario on card 1 and Yoshi on card 2. A card is a two-row scoreline now (portrait · NAME · score per side, the winner's row drawn as a plate·rail·W and the loser's dimmed whole, logo included). **The rows are the authored SIDES, never away/home** — side 1 is the top row on all five, so the eye scans one column and counts; away/home is faithful to a box score and destroys exactly that. Three markers, any subset, but **`-row` and the per-node dim are EXCLUSIVE**: a row group whose children were also dimmed multiplies to 0.2, which reads as a rendering fault rather than as a result. **A dim goes in `fill-opacity`, never `opacity`** — `setText` rewrites the opacity attribute on every slot it binds, which is why Classic's caption, dates, modes and stadiums were all rendering at full strength (`test_a_bound_text_slot_does_not_dim_itself_with_opacity`, now **unconditional** — it carried a seven-slot `_OPACITY_DIM_DEBT` allowlist across three other files, cleared 2026-09-12). **Clearing that debt is two verbs, not one**, because a dim the mount has always stripped is a number that was never once seen on air: HONOUR it where it still reads at broadcast size, DELETE it where it does not. Four of the seven were deleted — Classic's commentary sub-values wanted 0.92, an 8% cut on white under a 30px/800 name that no viewer resolves, and Classic's `scoreboard-l` `meta-date` wanted 0.65 on top of `cl-dim2`, **a fill that is already 0.40 alpha**, so the two multiplied to 0.26 (2.27:1 at 10px, the smallest type on the card). A stacked class alpha is the trap: read the `fill` before trusting the number beside it. Cards rather than a stub-column table because the mount hides the cards it has no game for: five self-contained cards with two shown reads fine, a table with three empty columns reads broken |
| **THE RESULTS TICKER'S CARD** | `public/layout/lib/ticker-mount.js` (+ `src/routes/layouts/ticker-mount.test.js`), drawn by `public/design/*/ticker.svg`. **A CLONE IS BOUND IN THE DOCUMENT, NEVER BEFORE IT IS APPENDED** — and this is the shape of the whole bug, not a detail: a detached node measures as NOTHING rather than as an error (`getComputedTextLength` → 0, `getBBox` → a zero rect, both measured in Chrome), and both of the mount's measurements guard on exactly that. Binding before appending therefore turned the auto-fit into `0 > maxw` — false, always — so `data-maxw` on every cloned name was inert in both packages from the day it was written. `VicklessFalcon` drew at its authored 17px, 100.75 units into a 98-unit budget, ending **1.2 units** short of the score plate; one longer tag and the name goes under it. Nothing in the shipped sample was long enough to show it, which is why the test pins the ORDER and stubs the two measurements *the way a browser answers them* — a stub that measured a detached node would be a test that could not fail. **THE CARD IS ANCHORED ON ITS CENTRE**, the same rule and the same `pinBesideText` as the Matchup strip: measured on a typical pair, the old outward-anchored names left **36.8 and 41.7 units** of open band between a name and the number it labels — 23% of a 340 card, *breathing card to card* as the names changed. The plate is fixed at the centre, each name anchors inward against it (every away name now ends at exactly 150), and each captain icon pins to the MEASURED outer edge of its name, so what varies is the outer MARGIN. That also retires the 34-unit hole `showCaptains: off` used to leave at each end — the names never depended on the icon. **WHO WON IS DRAWN, NOT DIMMED**: an accent rail flush against the plate on the winner's side plus the loser's whole row dimmed, over the shared `rowOutcome`. A tie marks nobody. The score was 16px under 17px names — the payload as the smallest headline on the card — and is 26 against 18; the card grew 340 → 424 to pay for it, so at the default gap the pitch is exactly 440 and four cards span the 1760 track. **THE PLATE IS THEME CHROME, OUTSIDE EVERY PART**: `score-group` hides on a game with no result, so a plate inside it left a live card with a hole where every other card has an object — which is the one thing a marquee cannot afford, the eye being parked on a repeating shape. The footer names the MODE, not the stadium (the Matchup card's call; `stadium` stays a bound part no shipped theme draws). **The mushroom field is on the RESULTS badge and nowhere else** — the one static region of a moving element; drifting marks behind the track would fight the scroll, and a sprite inside `card-template` is animated once per clone (21 of them on a ten-game pool). Its opacities run roughly double the dark-band elements' because the badge's ground is saturated accent and the mark's own red pixels vanish into it — **tuned by looking**: the first pass reasoned its way to 0.14–0.22 and drew four legible pink mushrooms behind the word RESULTS |
| Scoreboard live row (per-side cluster, count, diamond) | `public/design/default/scoreboard-l.svg` `row-live` + `bindSideLive` in `public/layout/lib/scoreboard-mount.js`. **The clusters are keyed by SIDE, not by role** (`s{T}-live-icon` · `s{T}-role` · `s{T}-stat-{0..3}-{label,value}`): side 1's cluster always describes side 1, and what flips every half-inning is which role it shows — so `AB`/`P` is a slot the mount writes, not a word in the SVG. Pinned to left=batter/right=pitcher (the older `bat-*`/`pit-*` slots, still bound for classic's Large board) the portrait under a producer's name in the header belonged to the other player half the time. **The stats come from `RioData.getStatsLine`** — the same resolver behind the Stat Bar and the Stat Card, so the four numbers beside a character here and the four on that character's own card agree and a change to "headline stats" lands once; `getTeamRole` answers the role. No character NAME at this size: beside the icon it was the only thing in the way of the numbers, and the portrait plus the ringed roster slot below already name them. **A 5-character value needs its column measured** — "1.500"/"128.1" are 51.2 wide at 17px Chivo Mono, which had 5 units of air in 56-wide columns; they are 68 apart now (percentages are whole since 2026-09-11, so `25%` no longer competes). Producer toggle `showStats` blanks the eight cells and **moves nothing** (this row has no reflow). **The row has ONE vertical centre (60)** — it had three (count 66, diamond 54, portraits 61), which an eye reads as one band each a few units off. Uppercase type centres on its CAP HEIGHT, not its em box: Inter's midpoint is 0.3523 × font-size above the baseline (measured — `getBBox` on a `<text>` returns the em box and will tell you 0.96) |
| Player Name sizing (an absolute px size, and a frame that only clamps) | `public/layout/lib/playername-mount.js` + `src/routes/layouts/playername-mount.test.js`. **`nameSize` IS the type size, and the frame is a CEILING** — 48 means 48 on every Player Name in the show, because `overlays.playername.*` is one global namespace and `requestedScale` takes no frame at all. That is the property the element rests on: the size used to be derived from the source's own HEIGHT, which made sameness hand-work (two names matched only while their OBS dimensions did — the stage's size-match row exists to repair one pair at a time), asked a producer to set type by dragging a corner in a unit nobody can do in their head (48px wanted an 88px-tall source), and let the PREFIX POSITION resize every name in the show by 1.53×. `fitToHeight` and `fitToWidth` only ever shrink, so the rule reads "48, unless this frame physically cannot hold 48", nothing is ever clipped, and the long-name fallback is the same mechanism on the other axis (per SOURCE, so one long name comes down on its own side while the other keeps the size that was typed). **THE PREFIX HAS ITS OWN SIZE** (`prefixSize`, default 24 = exactly half the name's 48, so nothing that shipped moved) — it was hard-locked at half, a fair default that was never a decision. Everything downstream is the **RATIO** between the two (`prefixRatio`), never a second absolute, so `--pn-scale` stays the single knob: both runs shrink together under a clamp, a preview's factor divides out of both at once, and the font border keeps tracking the type it sits on — the literal `* 0.5` on the tag's stroke became a second, silently disagreeing copy of that ratio the moment the prefix could be any size. `inline` takes the **taller** run for the same reason: baseline-aligning the tag inside the name's line box was safe only while the tag was guaranteed smaller. **NOTHING IS RESERVED WHEN THE PREFIX IS OFF** — the reserve is a function of the SETTING, so `off` is the name's line box alone (60px of frame for a 48px name, against 87 with a row above it) and the prefix SIZE stops mattering entirely. The one place space is held without content is a participant with no tag while the setting is ON, and that is deliberate: two sources framing a scoreboard must draw at one size and only one of the two players may have a sponsor. **BOTH CLAMPS ARE SILENT, AND THE HEIGHT ONE IS A TRAP** — 48px with a prefix row needs **87px of frame**, so a source dragged into the natural name-bar shape (wide and short) clamps from the first frame and *widening it does nothing*, because width was never what bound. Measured: 1600×60, 2400×60 and 3200×60 all draw 33.23px. Said in TWO places off one statement: `sizeNote` in the mount (which names the axis, because the two clamps have opposite remedies and widening is the instinct a height clamp defeats) and `stage/playername.jsx` on the console, both over `heightForNameSize` — **the mount's own inverse, imported not restated**, so the panel and the overlay cannot disagree about the threshold. The overlay reports through **`OverlayBase.setNote`**, the sibling of `setBlank` for "drawing, but not what you asked for": `data-prsh-note` + one `console.info` per CHANGE always, a corner badge **never on air** — visible in PREVIEW_MODE *and in DEMO MODE*, which is the widening that matters, because the console's preview iframe is a scale model at the element's DECLARED aspect and structurally cannot reproduce a clamp caused by the shape the producer dragged the real source into. Demo mode is the real OBS source while a scene is being built, and it carries no new risk: if it is on during a broadcast then canned fixture data is already going out. Its numbers are the SOURCE's, never the page's — `final / asked` is a ratio, so a preview's factor divides out and the badge never quotes a size nobody can act on. The WIDTH clamp is reported without a number on purpose: whether a name overflows depends on its glyphs, and re-measuring text here with a canvas would be a second implementation of the fit, in a different renderer, free to disagree about the exact case it exists to explain. It still does not min-fit a canvas (`Math.min(w/REF_W, h/REF_H)`) the way drawn artwork does — that aspect-locked the name to 4:1 and scaled the empty margin with the glyphs. **Three rules that read as cosmetic and are not**: padding is a fraction of the TYPE (10%), not of the frame, because what it makes room for (the font border, the text shadow) scales with the type and a frame-relative padding gives a small name in a tall box a huge margin; the scale is CONTENT-AGNOSTIC (it reads the prefix-position setting, never whether this participant has a prefix), because measuring the ink puts `rjb` and `MattGree` about 2× apart; and a PREVIEW is a scale model that has to be TOLD (`previewScale`, `?preview=1`), because ScaledIframe hands the overlay a genuinely smaller viewport, so an absolute size would draw a 48px name in a half-size box — proportionally double, on the one screen a producer checks fit against. Inside a container the factor is 1 and must be: `fitHostForPreview` already scales the whole host there. The inline row is `nowrap`: the fit solves for an EXACT fit, so a wrappable row broke onto a second line at 112 units in an 88-unit frame. The blur half of the text shadow is published by `overlay-base.js` as `--text-shadow-blur`/`--text-shadow-color` beside the composed `--text-shadow`, because a composed string cannot be multiplied and re-deriving `effTextBlur` here would be a second copy of the override rule. **THE TEXT SHADOW IS A `filter: drop-shadow()`, NEVER `text-shadow`**, here and on the Event Header: whether `-webkit-text-stroke` feeds `text-shadow` is undefined, and Chrome casts the halo from the STROKED glyph while OBS's CEF does not — so one setting drew a dense cloud in the console preview and almost nothing on air, which presented as "the shadow is missing in OBS" and was found by switching the BORDER off. `drop-shadow` is defined over rendered alpha, so the two renderers agree; it is built from the published parts because `drop-shadow(none)` is invalid. **The source is created at 800×200**, the top of the plausible range rather than the middle: dragging a browser source down is lossless and up is a bitmap upscale, so the default should leave only the lossless direction. One size, three readers (the container member box, the picker's `fitsContainer`, the source `addBrowserSource` creates) — never declare it twice |
| **A BROWSER SOURCE HAS TWO SIZES**, and OBS never says which one moved | `src/lib/obs-transform.js` (`renderFactor`/`stretchOf`/`redrawPlan`/`rescaleForSource`) + `stage/resolution.jsx` + the rack's `StretchBadge`/`StretchNotice`, pinned by `src/routes/production/stretch.test.jsx`. The INPUT's width/height is the resolution the page renders at; the scene item's transform is what that finished texture is then scaled to. Dragging a handle — the obvious gesture for resizing — moves only the second, so what goes out is a resampled picture of the overlay rather than the overlay, and **neither program says a word**. **BOTH DIRECTIONS COUNT, and that is not a quality feature**: shrinking a texture is lossless, but an element that draws type at an absolute size (the Player Name) is drawing a 48px name at 24px on a half-scale item — the setting and the broadcast disagree with nothing anywhere to say why. Redrawing re-renders the page at the size the item occupies and takes the scaling out: **the box does not move or change size**, so an element that fits its viewport looks identical and merely sharper, and fixed-size type comes back to its real size — which is exactly what a producer means when they drag a Player Name box (the box got bigger, the name did not). **`EventSubscription.All` IS NOT ALL** — obs-websocket sorts a few high-volume events outside the default mask (4095) and `SceneItemTransformChanged` (1 << 19) is one, so a client asking for "everything" is never told a source's geometry moved; `connect` opts in explicitly. It failed the way an unsubscribed event always does, silently and asymmetrically: the verdict was right at every scene mirror and then frozen, so a badge appeared on a dragged source and would NOT clear when the producer fixed it. The volume is answered in the handler, not by declining to hear it. **Only the Player Name's rows may carry the warning** (`SCALE_SENSITIVE` in `placements.js`, shared by the badge and the stage row so the two surfaces cannot disagree): every other element fits its artwork to whatever viewport it is handed, so a scaled item costs it sharpness alone, and amber over most of a rack for a fidelity note is how a producer learns to ignore amber. The verdict is mirrored on the scene item (`mapItem`) because `GetSceneItemList` already carries every transform and the mapper was throwing it away — **the transform itself is still not mirrored** (see `matchSceneItemSize`); this is a derived verdict, refreshed off `SceneItemTransformChanged` only when the ROUNDED verdict changes, so a ten-second drag moves the rack twice. Because **an OBS input is GLOBAL**, the redraw re-solves every other item of that source to keep the size it has, or a source in six scenes would silently resize five of them. A **crop is declined** (it is stated in source pixels, so a new resolution moves what it cuts away) but still **warned** about, since a console that went quiet where it had no button would be quietest about the hardest cases. Deliberately **not** an app-wide banner: the two at the app root mean the broadcast is showing the WRONG THING, and spending that register on "the right thing at the wrong fidelity" teaches a producer to scroll past it |
| Text ON a port colour | `inkOn` in `public/layout/lib/port-colors.js`, exposed by the scoreboard's `applyColours` as the `--side{N}-ink` seam beside `--side{N}`. Computed, never fixed: port 3 is `#fdd835`, so the white that is right on the other three is unreadable there — and a producer can repaint any port from the Design tab, so no constant survives. Light ink is the fallback for an unparseable value, because every card this lands on has a dark ground. Lives with the palette rather than in a mount: every element that tints by port faces the same question, and two answers is how one overlay ends up with an illegible badge nobody sees until it is on air. `src/routes/layouts/port-colors.test.js` |
| **THE VERTICAL SCORECARD'S SCORE BLOCK IS A PICKER, AND `ROSTERS` IS WHY IT HAS TO BE** | `mainMode` in `designConstants.js` (`full` · `rosters` · `condensed` · `off`), `STACK` + `bindRosters` in `public/layout/lib/scorecard-mount.js`, `el-rosters` in `public/design/*/scorecard.svg`, pinned by `src/routes/layouts/scorecard-mount.test.js`. The block a producer wanted between Full and Condensed is "the teams and their nines, without the at-bat situation" — and it cannot be built by SUBTRACTING from the full block, because **the full block's inning marker lives at the head of the situation row it would subtract**, so hiding the diamond hid what inning it was. A block is the unit; the picker's values are whole blocks the theme draws, which is also why the Rosters/Bases switches that used to sit beside this control are not coming back. **THE STRIP AT ITS FOOT IS THE SITUATION ROW MINUS WHAT NEEDS A DIAMOND** — inning · count, left margin to right margin, spaced on its GAPS (three of 58 between four clusters, because groups of 3 and 2 dots measure differently and only even gaps read as one rhythm). A right-hand state column was the other candidate and is what the CONDENSED bar does; it works there at 40 units tall and would leave this 210-tall block with a marker floating in a hole. **FINAL takes the strip alone** (`r-count` hides): every other thing on it describes a pitch that is coming, and the badge sits in the middle of where the dots were |
| What goes in a side's LOGO WELL | `RioData.captainIconUrl` (`public/layout/lib/rio-data.js`) — the team logo, and failing that the side's **captain**, BY INDEX (`rio_captainIndex`, never a guess at slot 0). The well is the only square on a name plate, so an empty one is a hole rather than a fact left unsaid, and it is empty in the states a producer hits on purpose: a completed record with no MSB team, a fixture bound before first pitch, an asset pack missing that file. The Scoreboard has always fallen back this way and the Scorecard's three blocks did not; the rule moved into `rio-data.js` rather than being copied, so a third element that grows a logo well cannot invent a fourth answer |
| The card's OUTER RAIL (a GLOBAL, default OFF, and what turning it off MOVES) | `applyCardRail` (`mount-utils.js`) + `overlays.global.showRail`, resolved by both card mounts as **global with a per-board pin over it** — the same chain as `showLogo` — and authored in **Design → Display Toggles**, beside its two siblings. Pinned by the `applyCardRail` block in `src/routes/layouts/mount-utils.test.js` and the rail block in `scorecard-mount.test.js`. **IT IS PACKAGE CHROME, WHICH IS WHY IT IS NOT AN ELEMENT SETTING**: the accent bar down a card's outer edge carries no data and marks no boundary — it is the theme's opinion about its own edge — so it is one decision about the LOOK, and a card panel is the wrong place to take it. As a `LAYOUT_SETTINGS` entry it landed ungrouped after the Lower bars region on the Scorecard (an orphan row with no heading) and last in a flat list on the Scoreboard. Both layouts still DECLARE it in their `<meta>`, which is what lets a per-board pin exist for the case where one card wants it and the other doesn't. It is **off unless the producer asks**, a deliberate change to what every existing source drew. **`display`, not opacity and not height**: both mounts write the rail's opacity and height every frame (the Scorecard sizes it with `card-bg`, the Scoreboard melds it), so a switch expressed in either would be overwritten by the next layout pass in ONE mount only — the switch working everywhere except one card. **THE RECENTRE IS SOLVED, NOT APPROXIMATED**: a hidden rail leaves its lane behind as dead space on one side, so the frame is re-solved for equal gutters either side of `card-bg` — `viewBox.x = bgX + (bgW - viewBox.w) / 2`. The obvious shift (half the rail's own footprint) is right on two of the three cards and silently wrong on the third, because it assumes the theme was centred on the rail's OUTER edge: Scoreboard L's `card-bg` already sits on equal 16s with the rail hanging outside them, so that card is off-centre WITH its rail and dead centre without — and "correcting" it moved a centred card 4 units right in a source the producer had already placed. The three answers are 3 · 0 · 4. **Off the AUTHORED geometry** (`data-basevb` on the root, `data-basebox` on `card-bg` — the `data-basefs` trick, mount-written so they need no `_MODIFIERS` entry), because Scoreboard S melds its `card-bg` width and a shift solved against the live one would slide the card every time a segment came or went. The rail coming BACK restores the authored frame and does not recentre: where the card sits with its full chrome on is the designer's call. Only `default` draws a rail; a theme that draws none ignores the setting, the way a size with no roster band ignores `showRoster`. **Not under Design's Card Chrome**, which is gated on the package leaving the app palette something to paint — hiding a rail works on any theme that draws one |
| **THE SCORECARD'S FOOTER IS TWO FACTS ON ONE LINE** | `footerLine` (`public/layout/lib/scorecard-mount.js`, exported and unit-tested), `showStadium`/`showDate` in `designConstants.js`, the single `el-stadium` band in `public/design/*/scorecard.svg`. Stadium and Date are peers, so each gets its own eye like every other band here. Where they go is **not a third question**: a `footerLayout` picker offering "one line / two bars" shipped and came straight back out, because stacking two short centred strings under six other bands spends 88 units saying what fits in 44 — the second arrangement is the worse one in every state, so the control could only ever make the card taller and no better. (Its removal took the two-master `showWhen` back out with it; the gate names ONE master again.) **ONE COMPOSITION, ONE BAND, ONE STRING** — filter the parts the switches allow and join them, so a lone value needs no case of its own and `off` needs none either: an empty string is what makes the band self-hide. **A LIVE HUD GAME HAS NO DATE**: `date_time_start`/`date_time_end` are written only by `apply_completed_game_to_state` (`server/rio/provider.py`), so the date is blank on a local HUD feed and fills on an API or completed game. The mount falls back end→start for the same reason the setting exists at all — a date that only appears at the final out is not worth a switch |
| The FINAL badge's colour | **One seam, `--final-badge-color` over the package accent**, on every badge the `default` package draws — the Scoreboard's two sizes and all three Scorecard score blocks. Two of the Scorecard's three drew a white `rgba(255,255,255,0.14)` pill and one drew `var(--accent)`, so the same badge changed colour when a producer cut between blocks; and because every badge in the package painted straight from the accent, the `finalBadgeColor` setting the Scoreboard advertises in its own `<meta>` reached **nothing in this package at all**. The Scorecard now declares the key too. Classic was already spelled this way (`var(--final-badge-color, #ef4444)`), which is what made the inconsistency a `default`-only bug and easy to miss |
| Scoreboard linescore + the game's meta | `public/design/*/scoreboard-l.svg` + `bindBox` in `public/layout/lib/scoreboard-mount.js` (column placement is `layoutBox`, shared with the Scorecard from `mount-utils.js`). **Default L has no `row-final`**: the completed-game meta was a band of its own and the game mode a line under the scores, so the card carried ~100 units of thin centred strips saying three short things, each alone in a 744-wide row. All three moved into `row-box` as a right-hand **meta pane** (568..772) beside the numbers they describe, paid for by deleting the away/home NAME column — both names are already the largest type on the card a few rows up, so a table repeating them spends its widest cell on what the eye just read (Scoreboard M was built this way first). The mode is therefore drawn only while the linescore is, which is from the first frame of a live game (Rio reports the current inning in its inning-score arrays) and always on a completed one. `meta-main`/`meta-date` self-hide, so a live board shows the mode and the stadium and nothing else. The classic package still composes with a `row-final`: **the row is part of the stack contract, not a thing every theme owes.** The **ELO cluster is deleted** (2026-08-29) — a one-game rating swing resets every season and was holding the two widest thirds of the old row; `showElo`, the `elo{1,2}-*` slots and their contract entries went with it, and the provider no longer writes the `*_elo` keys either |
| Add/modify a Layout or element overlay | `public/layout/<group>/` + `public/layout/lib/*-mount.js`, register in `src/routes/layouts/designConstants.js` (+ `src/routes/production/elements.js` if a Production element) |
| Sample data / demo mode | `public/layout/preview/*_sample.json`, `public/layout/lib/overlay-base.js` (`sample` option), `src/routes/production/sample.jsx` (switch + banner) |
| Theme/design packages | `public/design/`, `server/design_packages.py`, `public/layout/lib/svg-theme-engine.js` |
| Design tab (presets, global design controls, previews) | `src/routes/layouts/design.jsx` (controls + sticky previews), `presets.jsx` (the presets strip), `presets.js` (snapshot/apply/legacy-preset normalize, pure + tested), `globalReach` in `designPackage.js` (each section names the elements it reaches under the active package — data in place of the paragraphs that used to explain it), `Settings.ApplyBatch` + `v1.settings.apply_batch` (one commit for many keys), preset logos in `server/api/v1/branding.py` (`/branding/presets/{id}/logo/{capture,apply}`). **A logo change bumps `overlays.global.logoRev`**, which `OverlayBase.brandingLogoUrl` puts in the query string — the file is replaced in place, so without it an on-air overlay never refetched. **Border Radius is gone**: no theme reads `--border-radius` (`rx` can't take a var), so it was a control that moved nothing on any package |
| **A FONT CHANGE MUST RE-MEASURE EVERY FIT** | `fontsKey` in `svg-theme-engine.js` + `fontGeneration` in `overlay-base.js`. The theme engine's auto-fit cached by TEXT alone, and a new face lands a render AFTER the one that names it (the mounts' `document.fonts.ready.then(refit)` sees an already-resolved promise), so a name that fit in Rajdhani ran over the score in a wider face until the source was reloaded. The cache now keys on the three role vars plus a generation OverlayBase bumps on every `loadingdone` (then re-renders). A fit a mount computes itself from a FIXED authored x has the same bug one layer up: the Stat Card's line started at `data-x-labelled` whatever width "GAME" drew at (`lineTextBox` now takes the label's measured end), and the Character Spotlight's mini stats were pixel-sized for Chivo Mono (`fitMinis`, one uniform factor) |
| The three type roles (what a name, a caption and a score are set in) | `public/layout/lib/rio-theme/tokens.css` (the faces) · `applyTypeRoles` + `TYPE_ROLES` in `public/layout/lib/overlay-base.js` (resolution, the Google fetch, and the rule that a palette clear does NOT clear type) · the three defs in `src/routes/layouts/designConstants.js` · the Typography section in `src/routes/layouts/design.jsx` · `overlays.global.{display,body,mono}Font` + the v5 split in `server/settings.py`. Tests: `src/routes/layouts/type-roles.test.js` (behavioural), `tests/unit/test_design_package_svgs.py` (per-theme contract), `tests/unit/test_settings_migrations.py` (the split). **A layout offers a role only if its `<meta>` names it** — the post-game callouts declare `bodyFont, monoFont` and not `displayFont`, because their headline type draws at 800/900 and Rajdhani is fetched at 500/600/700; Player Name and the Event Header declare only `displayFont`, because it is all they draw. A themed layout names all three and the ACTIVE THEME narrows it per file (`typeRoles`, see the style-overrides row) |
| Figma round trip (designer edits a theme) | **Two inverse halves, and the seam between them is where bindings die.** `server/theme_compiler.py` + `scripts/compile-theme.py` take a designer's export → shipped theme; `server/figma_template.py` + `scripts/figma-template.py` take a shipped theme → the editable `design-templates/*.template.svg` the designer opens. Templates are **generated, never hand-maintained**. Figma silently ignores `<style>` CSS, `var()`, and href-less `<image>`, and keeps layer *names* rather than `data-*` — so the generator bakes all four out. `tests/unit/test_figma_template.py` runs the loop over every shipped theme and compares slot counts **per name** (a set comparison misses a dropped duplicate; the lower third binds 33 nodes across 20 names). A slot name the grammar can't spell can't survive a design tool at all — that test is what caught `box-away-R`. **A MODIFIER the grammar can't spell is the same bug, one layer down** and fully silent: the node survives, the binding it carried does not. `_MODIFIERS` is the one table of spellable modifiers — a mount that reads a new `data-*` off a theme node must add it there in the same change, or the round trip returns a theme where that behaviour is simply gone. Pinned by `test_round_trip_preserves_every_modifier`, which found four families at once (the vertical meld's `cardh`/`compact-h`, the stat card's four edge offsets, the lower third's `full`). A value with SPACES needs `_LIST_MODIFIERS` too — a layer name is split on spaces, so it rides on commas. Layers authored `opacity:0` are **revealed** in the template (a designer cannot style what the tool draws as nothing) and carry the bare flag `hidden`, which the compiler restores; a melding card gets dashed `scaffold=stage-*` boxes for each extent it grows to, derived from the meld attributes so they cannot drift |
| What the console calls side 1 and side 2 | `src/routes/production/sides.js` — `sideLabel` (standalone: an eyebrow, a control) and `sidePhrase` (mid-sentence, carries its article), both off `production.side_labels` (`numeric` \| `lr` \| `tb`, default numeric), authored in Settings → General. **Every surface that names a side goes through it**: the board desk's columns and score boxes, the subject rows, the container scope control, the controller URLs, the match draft cards, the Address Book's **Side** column, and the `?team=` variant label in `instances.js` / `addsource.jsx` — so a rack row and the panel it opens cannot disagree. **Geometry keeps its literal words**: the container anchor menu, `PP_LOCATION_OPTIONS`, `?dir=left\|right` and text alignment describe a position on a canvas, not a side of a game. A **side pin is stored as the number 1 or 2** (`prefs.side` on a participant) — it was the literal string `"Team 1"`/`"Team 2"` in settings, which two server readers string-compared against; that pair is migrated away at boot, so only the caption moves and nothing compares against a label any more |
| Controller-port colours (the side tint five overlays share) | `public/layout/lib/port-colors.js` (overlay runtime) + `src/routes/layouts/designPackage.js` (`resolvePortColors`/`usePortColors`, app runtime) — **one palette, resolved the same way in both**: the producer's `overlays.global.port{N}Color` (Design tab → Controller Ports, `design.jsx`) → the active package's manifest `portColors` (`_port_colors` in `server/design_packages.py`) → `DEFAULT_PORT_COLORS`. Never a per-element copy: it was one, on the Character Spotlight, and recolouring port 1 there left the other four overlays red |
| Production console (rack/stage/rail) | `src/routes/production/{rack,rail}.jsx`, `stage/`, `desks/`, `kit/`, `elements.js`, `placements.js` (row identity), `addsource.jsx` (Add picker). **MATCH IS THE RACK'S FIRST SECTION, THEN BOARDS** — a night is authored before any board matters, and the Match row is the one that can say how much of tonight is left (`useMatchDeskMeta`: `3 waiting` · `all played` · `none waiting`, never a fixture's id and series — `M1 · 0–0` named the first match ever authored and quoted a number that stays 0–0 all night on a Bo1). Boards led until 2026-09-13 on the argument that "the fixture, the capture and the bracket all act ON a board"; Capture and Bracket have both since left the tier, so what it really said was that a desk acts on a board, which is true of every desk there could be. They keep SEPARATE sections either way, because membership differs (one Match desk forever vs one to three boards a producer adds and removes, so a shared `+` could only add one of the two kinds). A board row carries `GameStageChip` — the rack is what the app opens onto, and a board holding last night's finished game was otherwise indistinguishable from one mid-inning without opening the panel; `all played` over `OLD · Scoreboard 1` IS the cold-start readout, which is why there is deliberately **no app-wide banner** for it (the two at the app root mean the broadcast is showing the WRONG THING, and on a cold boot nothing is on air and often OBS isn't even open). **A SIDE COLUMN IS THE VIEWPORT'S, NOT THE ROW'S** — the rack and rail are `sticky` viewport-tall columns whose list is `flex-1` inside the panel's own height, never a second copy of that arithmetic. They were a fixed `h-[calc(100vh-13rem)]` scroll box (a viewport-sized height reaching for a viewport-tall column) inside an `h-full` panel stretched to the grid ROW, and nothing ever pinned it: over a tall stage the panel's box ran the row's full 1397px while its list stopped at 492, leaving ~900px of empty card under it, and the whole column scrolled away while the producer worked in the stage — so the rack, which is how you reach anything, was off screen exactly when the panel you scrolled to see was on it. Sticky applies **only where the column exists**: the rack at `lg`, the rail at `xl`, each matching the track it appears in, because below that the grid is one column and a sticky viewport-tall block would pin one section over the whole screen. A viewport-tall column that starts ~146px down the document must overflow at rest by that much — one small scroll pins it and it fits exactly; that is the trade, not a bug to "fix" by shortening it back. |
| One board's console panel (mirrored game state, lineup readout, `side_reason`, corrections, rename) | `src/routes/production/desks/board.jsx` (`SidePanel`/`RosterGrid`/`useSideTeam`; **which layer seated the sides is a BADGE** — `sideReasonStatus` → `FROM MATCH`/`PINNED`/`LAST GAME`/amber `BY HAND`, with the sentence in its `title`. Every other fact on that header rule is a badge (`HUD`, the mode, the game's own stage an inch above), and four of the five words in "Sides from the bound match" were the same on every board. Its subject is the `Swap sides` button beside it, which keeps its full name because the rail card spends the same words on the same press — so the badge carries no `SIDES` eyebrow of its own) + `boards.js` (`boardDeskId`, `useActiveBoards`), rows in `rack.jsx` (`useRigRows`, `RigSection`, `RowRemove` — add/remove a board), bodies in `production.jsx` (`deskBodiesFor`), face in `quickface.jsx` (`deskQuickFace`) |
| **A BOARD RUNS TWO CLOCKS**, and the panel is built on that | The *game* (`score.{N}`: empty → live → final/stranded, feed-owned, watched) and the *match* (`match.{M}`: draft → live → post + `decided`, producer-owned, driven). They are `BoardSubject` in `desks/board.jsx` — **ONE ROW, because they share a subject**: the game on the left (stage chip · side · score · side · inning), the fixture on the right behind a rule (`M{id}` · series · `DECIDED` · round · between games · unlink). The game was a bare line of text beside the match's bordered slot, so the pair never read as two halves of one question; giving them one shape fixed that and made the next problem — **two full-width bars spending their width on the SAME TWO NAMES**, ~400px of dead air in each. **The names were a COPY**: the Match projector writes `match.{M}`'s participants into `score.{N}.player.{T}.rioName` (`_FEED_SHARED_KEYS`), so the second bar reprinted the first's. Nothing else was dropped — the three unbound states (`ROTATING`, `UP NEXT` + take, `NO MATCH`), each of which was a full-width dashed bar holding about a chip's worth of content, are tails on the game's row too. **THE MERGE IS CONDITIONAL AND THE SPLIT IS THE POINT**: `agree` compares the two pairs of names, and a board where they differ goes back to two bars, because then the DIFFERENCE is the content and one row could only show one pair — which is the one thing a producer resolving a `match_conflict` must not be handed. It compares names rather than trusting the flag alone: a fixture bound with **no participants yet** is a real state the identity gate says nothing about, and splitting there is how "Side 1 vs Side 2" under a live game tells a producer they bound an empty draft. The fixture's decided badge is **`DECIDED`, not `FINAL`** — the game chip on the same row spends FINAL on a game that reached its last out, a captured Bo1 is both at once, and two identical badges a hand's width apart differ in exactly the way the producer is checking (they were on separate rows when that badge was written, which is the only reason one word served both). Wrapping degrades to the old two-bar layout on a narrow panel, which is why the game half carries a `basis`: as bare siblings the `flex-1` names would truncate to nothing rather than let the tail wrap. **Where the game is up to lives on the CHIP, not in the meta** — `FINAL`/`STALLED` used to *replace* the inning, so the row answering "is this still going" answered in the same dimmed type as "Top 5" and a finished game lost the inning it finished in. **The chip carries its state in colour** (`GAME_STAGE`) and is the one place on the console allowed to: the status palette in `kit/chip.js` describes what a SOURCE contributes to the broadcast, a game's lifecycle is a different axis, the two are never drawn together, and nobody reads "LIVE" in front of a score as "enabled in the program scene". It keeps the flat label shape rather than `StateChip`'s bordered glowing pill so the two stay distinguishable. Amber on STALLED is deliberate reuse — amber means "you'd want to know before this is on air" everywhere, and a feed that lost its game is exactly that. **The amber chip's label is not a synonym for FINAL** — both stages mean "this will not change again", so the only thing the chip has to say once a game stops is which of the two it is, and `ENDED` (its label until 2026-09-09) was the word for the amber case that most sounds like the grey one: a final game has a result to capture, a stalled one has none and its score and inning are a frozen mid-game frame. The state key stays `stranded`, which is the better word for the condition and the wrong one on a baseball scoreboard |
| **A HUD GAME ENDS WHEN ITS STAT FILE APPEARS**, not when a flag flips | The HUD feed has **no final frame** — Project Rio stops writing and its last frame stands — so `game_over` never arrives and a finished local game read `live` forever: the board sat on "8–8, Bot 6" with the real result captured directly underneath it, saying LIVE, and the turnover bar that exists for that exact moment never appeared. `boardLifecycle` takes a `captured` input (`boards.js`), checked **first** because a parsed final box score is the strongest evidence there is. The server already knew this — auto-capture is built on the same signal (`server/postgame_watch.py`) — so this is the console agreeing with it rather than holding a weaker opinion. **The capture must be THIS game's**: nothing clears `postgame.{N}` on a new game, so a bare `present` would mark game 2 of a Bo3 final the instant it kicked off, on game 1's box score |
| **RIO WRITES TWO FRAMES PER EVENT, AND ONLY ONE OF THEM DESCRIBES A PLAY** | `HudObj.event_resolved` / `inning_end` / `game_over` in `server/rio/pyrio/stat_file_parser.py` (+ `pyrio/tests/test_hud_game_over.py`). `Event Num` is `"120"` while the play is in flight and `"120b"` when it resolves — the pair is why `Previous Event` on a `…b` frame is that very event. **`Outs` + `Num Outs During Play` may only be ADDED on a resolved frame**: in flight `Outs` has already absorbed the last play while `Num Outs During Play` still describes it, so the sum counts one out twice and says the half is over with two outs and a batter at the plate. That is invisible until a second condition also holds, which is why it surfaced as a MERCY bug — a board on a 10-run margin flipped Final → Live → Final with every HUD write, once per in-flight frame after the second out, while every resolved frame answered correctly. Measured over 773 stat files: 0 wrong verdicts on resolved frames, 533 games carrying at least one wrong in-flight frame. In flight, `Outs` alone answers (it resets at three, so a half being played is never over). The walk-off branch is deliberately NOT gated on it — it asks about the score, and a run that ends a game does not wait for a resolution. **`Half Inning` is the BATTING TEAM's away/home id, not top/bottom** (`batting_team()` returns the same number), so `bottom` resolves through `first_batting_team` like the scores do: read as away/home it inverted both halves whenever HOME batted first, putting the ungated walk-off branch over the whole of the first-batting team's half and missing the real walk-off in the second-batting team's. **A KNOWN GAP, LEFT ALONE**: a frame's scores are PRE-play, so the frame that ends a game on a scoring play shows the score before it — 133 of those 773 games answer False on their own last event (every walk-off, and a mercy clinched by the final swing). Under-detection is this predicate's chosen failure direction and `boardLifecycle` covers it with `captured`; do not patch it by adding `Previous Event.RBI` to the score, which is not runs scored |
| The end of a game (capture · clear · take next) | `TurnoverActions` in `desks/board.jsx` — the three presses of that moment, **in press order**, and **they ride `BoardSubject`'s row as its third group** (game · fixture · presses, each behind its own rule). They were a bordered strip of their own, which was right while the strip opened with a sentence and read as a floating button the moment the prose came off — a press needs to be attached to the thing it acts on, and that thing is the row above it. They were in three places and the wrong order: the take inside the fixture slot *above*, the capture a region at the *bottom*, only the clear here — and **capture is what advances the match and credits the series** (`server/postgame.py`), so the panel printed the consequence a screen above its cause. **A STATUS A PRODUCER CANNOT ACT ON FROM WHERE THEY READ IT IS A RIDDLE**, and this bar was the console's worst one: it opened with `Captured automatically.` — a receipt for something that already happened — over a board whose chip an inch above said `OLD`, where nothing had been captured this session at all. **THE BAR NOW SAYS NOTHING — IT IS THE PRESSES.** Naming the CONDITION instead (`turnoverLine`, deleted 2026-09-14) only moved the duplication one surface over: the condition is the `GameChip`'s job in the `GameChip`'s own words one row up, so the panel's top three rows opened with the same matchup twice and then prose about it, and a producer read the same scoreline three times to reach a button. Everything the strip could say is owned by a surface that says it better — the chip (`OLD`/`STALLED`/`FINAL`, each with the sentence in its title) and the Post-game region (`AUTO` + both names + the final score) — so what is left is the verbs **at their natural width** (`w-fit`: nothing on a full-width desk gets `flex-1`), and the one fact no other surface holds, why no take is offered mid-series, is a `GAME 2 OF 3` badge rather than the sentence it ended with. **ONE PRESS, ONE PLACE — the clear does not MOVE.** This group returned null off `isStaleBoard` and a second `Clear game` at the foot of the game-state region covered every other time, so there was always exactly one clear on the panel and it was in a DIFFERENT PLACE depending on lifecycle. What a producer saw was the button moving: press Clear here, the board empties, and the clear they just used is now at the bottom of the panel. The region's copy is gone and this one renders ALWAYS; only Capture and the take stay gated on `isStaleBoard` (`atTurnover`), because you do not hand a board on or read a stat file mid-game, while the clear is the verb a board always owes. A control that relocates when its subject changes state is its own confusion — the same argument the Match desk's `Clear` makes about its two faces. **Capture is offered on `final` alone** — a `stranded` game has no result and a `restored` one's belongs to a previous session, where the recovery is the post-game region's file picker. **Exactly one FILLED press per panel, and it is the forward move**: taking a fixture clears the board on its way (`bind_board`'s `_clear_superseded_game`), so where one is waiting the take wears the fill and Clear stands down to a ghost beside it — with nothing waiting, clearing *is* the move and takes the fill. `Clear game state` lost its `state` in the same pass: that is the app's word for the keys under `score.{N}`, not the producer's word for what is on their scoreboard, and the bar has to read as an instruction. **THE RELEASING CLEAR NAMES THE FIXTURE IT TAKES OFF** (`Clear & unbind M{id}`, was `Clear board`): two names for two acts is the right shape, but the whole difference between them was one word for a container the producer has to already know includes the match — so "does this also detach the match?", the one question a producer asks that button, was the one it answered least. **Do NOT answer it by offering both** — at any moment only one is correct (an undecided fixture must keep its binding, and over a decided one "clear but keep the match" is an option whose visible outcome is *nothing changed*, because the projector immediately repaints it), so a second button would always be the wrong one of the pair. The other act has its own control where its own fact is: the fixture tail's unlink. **The take is not gated on a decided fixture**: that gate is right for a Bo3 and wrong for the Bo1 `default_match()` makes, where the producer's next press was hidden behind a condition unmet *because* the capture that decides it had not happened. **EXACTLY ONE TAKE ON THE PANEL, ACROSS ALL THREE SITES THAT CAN DRAW ONE** — the fixture slot's end-of-life take (bound + decided), the UP NEXT ghost (unbound), and this bar's. The rule reconciled only the first and third, so a **stale, unbound** board with a fixture waiting drew the ghost's take and the bar's, two identical `Put on board` buttons an inch apart — which is not an edge case but the cold start, where last night's game sits on a board whose fixture was retired with it. The bar's take is gated on the board being BOUND, the one state the other two cannot cover |
| Count dots, everywhere one is drawn | **3 · 2 · 2** — the terminal value of a count is never drawn: a fourth ball is a walk, a third strike and a third out end something, so each resets rather than lighting a dot. Three runtimes have drawn it wrong in turn, each for a different reason, which is why it is stated here rather than in any one of them: the board desk drew 4/3/3 (`CountDots`, `desks/board.jsx`); `scorecard-mount.js` LOOPED 4/3/3 over themes that were authored 3·2·2, so the extra iterations bound nothing on classic (a missing slot is a no-op) and lit a permanently dead dot on `default`, which was the only package drawing one. **A dead dot looks exactly like an unlit one**, so it survived both packages' review. The mount now hides the retired slots (`COUNT[].retired`) as well as leaving them unbound, because a package authored before the convention still declares them and a producer has nothing on the console that could turn one off |
| A control the feed owns | **Inert, not greyed** — `FEED_OWNED` in `desks/board.jsx` (`disabled:opacity-100 disabled:cursor-default`) over `disabled={d.feedLive}`. Every cell of the mirror is in the per-frame `SetBatch`, so over a live game a typed correction survives until the next frame and no longer; the controls stop taking input, and **nothing says so**. Letting the primitives' `disabled:opacity-50` through was tried and removed: it dulls the count dots, which are the one part of the panel carrying meaning in colour, to make a footnote out of something a producer learns once. The overrides beside them (`rioName_override`, which side is home, the swap) really do outrank the feed and stay live |
| Why the bottom half of the board desk is laid out the way it is | **Nothing on a full-width desk gets `flex-1`.** `ActionRow` sizes its buttons equally, which is right on a rail card and wrong the moment the surface is wide — a lone Clear became a ~1200px banner and the two post-game buttons 600px each, so three regions read as a stack of banners. `ActionRow` takes **`fit`** (natural width, left-aligned) for exactly this; Stadium is `w-56` and the mode picker `max-w-lg` (the longest season name PRSH has seen) for the same reason. **The game mode is not a board property** — it is `scoreboards.binding.{N}.stats_tag`, so it is the Games region's first row, not a field floating above that region's header. **Regions are ordered by how often a producer touches them**: game state → Games → Post-game → the board's running order, which is set once a night and goes last (and draws itself only on a rig with more than one order, so it takes its own section rule with it when it hides). **A BOARD IS RENAMED WHERE IT IS NAMED.** A `Name` field used to sit in that last region, at the foot of a body four regions long, setting the 15px title printed at the top of the very same panel — the console's longest distance between a value and its control, reached by scrolling past everything the board is doing. It is the panel title itself now (`PanelShell onRename` + `titlePlaceholder`, wired by `deskBodiesFor`): no frame at rest, a border on hover, the input treatment on focus, sized by `field-sizing: content` (a `ch` estimate came up ~5px short of the ink, because `ch` is the width of a zero and that title is Rajdhani caps on 0.08em of tracking). The **stored** alias is the value and `Scoreboard {N}` the placeholder, drawn at TITLE strength — that name is what the board is called until someone calls it something else, so greying it would grey out the panel's entry point on every board nobody has renamed. Only a desk that hands the stage a `rename` gets a typeable title; every other stage title is DERIVED, and offering to type over one would promise a name the thing does not have. **The pool is what earns a region, not the transport** — the Games region rendered on every board carrying a badge, a playback sentence, a re-read and the mode across four lines, and on a HUD board its body (`../games`) is empty, so a board with one possible game from one file spent a labelled region and a divider saying so. All four moved onto the Game-state rule (`ModeRow`, which is now the whole feed control) and the region renders only for `transport === 'api'`, where a pool and a playback surface are real. `KitColumn`'s header wraps for the same reason — a rule that carries a control group must take a second line rather than truncate its subject. **EVERY REGION DRAWS ITS OWN RULE, and `KIT_SECTION` is how.** The desk hand-rolled ONE `<Divider>` under Game state, so on an API board the panel's only line landed above Games and Post-game ran straight on out of the pool surface, while on a HUD board the board's *name* ran straight on out of the capture. `KIT_SECTION` is the kit's one statement of what a section's rule is, `first:` resets included — which is exactly what lets the Games region be absent without leaving a rule hanging at the top of the stack. **One verb, one name, one button**: the region's `Clear game state` hatch is DELETED — it and the turnover group's were the same `resetGame` standing down for each other by lifecycle, which is how one button came to have two homes (see the turnover row) |
| Shared containers (definitions, roster) | `src/routes/production/containers.js`, `stage/container.jsx`, `public/layout/shared/container.html`, `production.container_defs` in `server/settings.py`, `server/api/v1/layouts.py`. **A member's board and side come from the container it is nested under** — one hook, `useMemberScope(element, placement.slot)`, read by the rack radio, the source strip, the rail faces and the stage pickers; a lookup by element answers with the first roster, which on a mirrored pair is a coin flip. A `containerScoped` member has no standing intent, so its remembered payload is never replayed over that scope (`feeds.js`, `feeds.test.jsx`) |
| Per-side stat line — the PAIR (`statsbar` + `statscard`) | **One mount, two elements, named by shape**: `statsbar` is the wide 452×118 bar with its own `?team=` source (`public/layout/scoreboard1/statsbar.html`, theme `statsbar.svg`, settings `overlays.statsbar.*`); `statscard` is the 2×2 380×240 card that is a container member and nothing else (`statscard.svg`, `overlays.statscard.*`). Both are `lib/stats-card-mount.js` — the caller passes `settingsType` + `svgElement`, so the two are themed and configured independently. **There is no `stats` any more**, in any layer: the id was ambiguous the moment the card existed, and it had also once named a *third* thing — a fed 325×120 DOM bar in a seeded "Stats Bar" container, deleted 2026-08-23 (`lib/stats-mount.js` + `shared/stats-feed.html`). Renamed across all five layers 2026-08-23 (element id · `overlays.*` namespace · layout file · layout type in `_TEAM_VARIANTS`/`_DISPLAY_NAMES` · theme element + `theme_contracts.CONTRACTS`), with a `Settings.Load` migration moving `overlays.stats` → `overlays.statsbar`. **A design package that still ships `stats.svg` silently falls back to `default`'s bar** — element-by-element fallback never errors, so re-export any package authored before the rename. **THE BOTTOM LINE IS THE LONGEST STRING ON EITHER CARD AND WAS THE SMALLEST TYPE ON IT**, because it was CENTRED beside a "Game" label pinned at the left edge: a centred line grows out from the middle both ways, so clearing that label costs the same width again on the right, unused — 244 of the card's 340 units, and a centred 13.5px line under 24px numerals on the bar. Left-aligned after the label it runs to the card's right edge, and an ordinary pitching line renders at its full 18px instead of auto-fitting. The label is CONDITIONAL (`line-label` is bound only for a HUD game's line; an API board's "Tournament Stats" arrives as the line itself), so the theme authors both left edges against one right bound — `data-x-labelled` / `data-x-bare` / `data-maxr`, resolved by **`lineTextBox`** (`public/layout/lib/mount-utils.js`), which takes **all three or none**: a theme that lost one would switch layout models on a game boundary. Pinned in both runtimes — `src/routes/layouts/mount-utils.test.js` and `test_a_left_aligned_caption_line_declares_all_three_edges` — and, like every `data-*` a mount reads, the trio is in `_MODIFIERS` (`server/theme_compiler.py`) or the Figma round trip returns a theme with the behaviour silently gone. **THE BAR'S FOUR CELLS ARE NOT EQUAL**: each stat carries `width`, the characters its CATEGORY needs at its widest (`RioData.getStatsLine` — AB 3 · AVG/SLG 5 · SO% 4; IP 5 · ERA 4 · K% 4 · AVG 5), and `layoutStatCells` (`mount-utils.js`) divides the `stat-row` band (`data-span-x`/`data-span-w`) in that ratio. Equal columns gave a two-digit AB the cell of a `.429`, so the visible gaps between the batting values ran 41 · 23 · 14; weighted they run 35 · 39 · 37. **Per CATEGORY, never per value** — a column sized to the number in it would shove its neighbours when an AB ticked 9→10, the jitter `tabular-nums` prevents one digit lower; the columns move only between batting and pitching, under the batter-change dissolve (which is why the role is now part of the mount's `charKey`). **Precision and width are one decision**: percentages are whole and ERA carries one decimal (2026-09-11) because at `100.0%`/`12.00` budgets the numerals had to come down to ~28px to fit — so a format change moves its `width` in the same commit (`rio-data.test.js` checks the widest value each formatter can produce against its budget). Unruled rows only: the Stat Card's 2×2 and the scoreboard's live block sit inside drawn dividers, declare no band, and keep their authored columns — the same RULED/UNRULED line `layoutBox` draws |
| Container runtime (members, cross-fade, offsets) | `public/layout/lib/container-members.js` (the `MEMBERS` registry — add a member here, never control flow; **it imports nothing**, reaching each mount through a dynamic `import()`, which is what lets the console import it instead of regex-parsing it and what stops a container loading three.js to find out it doesn't need it) + `lib/fed-container.js` (the wiring) + `lib/container-layers.js` (retained layers, cross-fade, `memberBox`/`memberOffset`, `resolveFeed`). **An element is hostable exactly when the registry has an entry** — `containerHostable` was a second copy of that fact and is gone. **Scope is applied by the WRITER, never here**: the payload on the feed key is already scoped (`_scope_of` server-side, `useMemberScope` for a Push), so the runtime draws what it is given — a second place to apply scope can only disagree with the first. **A member reading state the shell doesn't listen to declares `watch`** (unioned by `memberWatches`): the shell wakes on its feed key, `score.N.*`, `postgame.N.*` and `tournamentInfo.*` and nothing else, so the Upcoming Schedule (queue + fixtures) and the Event Header (its Phase field's fixture) rendered once and then went deaf **inside a container** while their own dedicated sources — which declare their own `shouldRender` — updated fine |
| Container automations (rules, dwell, resting, reason) | `server/automations.py` (engine + `RESOLVERS`, registered on `State.hooks`), `server/state.py` (`_augment`), `src/routes/production/automations.js` (quick-add library), `stage/automation.jsx`, `tests/unit/test_automations.py` |
| Console with no OBS (catalog tier) | `src/routes/production/placements.js` (`catalogPlacements`, `useConsoleOffline`), `rack.jsx` (`CatalogSection`), `sourcestrip.jsx` (Copy URL), `addsource.jsx` (`open` vs `scene`) |
| Production OBS control / staging | `src/context/obs.jsx`, `src/context/staging.js` |
| Matching a per-side pair's OBS size | `src/lib/obs-transform.js` (the arithmetic — **size, never position**, and it never converts the target's sizing METHOD: a bounded item gets its box restated, an unbounded one gets a scale solved against its OWN cropped source, so copying a size can't silently undo a producer's bounding box or trip obs-websocket's zero-box validator), `matchSceneItemSize` in `src/context/obs.jsx` (both transforms read on demand — **transforms are deliberately NOT mirrored**, and the read happens at RUN time so a staged match commits the size the model has when it goes live), `sideSibling` in `src/routes/production/placements.js` + `flipSideVariant` in `instances.js` (the other half = same element and SCENE, other `?team=`, every SHARED variant axis intact — a `zs` scoreboard's twin is the other small one). **The board is a PREFERENCE, not a requirement**: a size is geometry and has nothing to do with which board a source reads, so the same board's half wins where there is one and otherwise the lowest-numbered board's, and the row names it (`Match Side 2 · B1`). Requiring the same board made the Size row vanish from BOTH panels the moment the stage's Board row re-pointed one half, though nothing about the two boxes in OBS had changed. **A per-half axis is dropped** (`sidePairVariant`, `PER_HALF_PARAMS`): `?port=` names which physical pad a controller source reads and the two halves of a pair are on different pads BY DEFINITION, so carrying it through the flip asked a pair to be something no pair can be — the Controller was the one per-side element whose panel offered nothing once its ports were pinned. Both ends of the comparison are reduced, never one. A port with no side still names no side and gets no row, row in `stage/sizematch.jsx`. **IT PULLS, NEVER PUSHES**: the panel resizes its own source, both halves carry the row, and a fed row is excluded because its source is the shared container's. **A `SCALE_SENSITIVE` pair is matched on BOTH sizes** — the input resolution first, then the transform solved against the dimensions the source is about to have. Copying only the transform is the whole of "how big" for every element that fits its viewport, and half of it for the Player Name, whose type is solved against the input: matching a redrawn 500×125 side against an 800×200 one put the boxes level and the names at 30px vs 48px, and introduced the very stretch the rack raises an amber badge for |
| Connections tab (what PRSH talks to outside itself) | `src/routes/connections/` — `connections.jsx` (page, the LAN card — `GET /network` (`server/network.py`) reports the setting AND the bind main.py actually made, so the card can say a restart is pending, plus the LAN URL to open on a phone), `kit.jsx` (the cards' shared shapes — `ConnCard`/`Section`, **one** `StatusPill` per card in its header and never a restatement of it in the body, `PathField`, `ToggleRow`), `rio.jsx` (Project Rio card = HUD file + its Follow toggle + Rio API refresh; MSB pack census as one tile per category), `obs.jsx` (websocket config — RECONNECTING stays on the console's `ConnectionPill`, so this is for changing the address, not getting back on air), `controller.jsx` (gc-overlay install + lifecycle + the live per-port previews, which carry no producer style — they answer "is the pad reaching PRSH", so the most legible drawing is the right one). **THE MEMBERSHIP RULE: if it doesn't point at something outside PRSH, it isn't on this tab** — that is what keeps it from growing back into the 995-line modal it was carved out of. Preferences (side labels, confirm mode, auto-capture, txt export) and the escape hatches (Logs, Reset boards and matches) stay in `SettingsModal.jsx`. It earned a tab rather than a desk because **a rack row's meta must read from State only** and a connection health summary costs three REST calls (`.claude/skills/production-console-contract/reference/desks.md`), and rather than a modal because every card here has a diagnostic readout you check AGAINST REALITY — drop files in, plug a pad in, start OBS — which a modal has to be closed to do. **A pointer to one of these settings is a doc that goes stale**: `WelcomeCard.jsx`, `obs.jsx`'s error string, `rack.jsx`'s no-OBS note and the board desk's rebind hint all name this tab now |
| Participant registry | `server/participants.py`, `src/routes/player_list/`. Three blocks per row: `identities` (join keys), `display` (**the resolve-by-copy payload projectors write into `score.player.*`**) and `prefs` (how PRSH *treats* this person — today `side`, the cascade's pin). A value no overlay draws does not belong in `display`; a new `prefs` field needs its key in `_PREFS_DEFAULTS`, a branch in `Update`, **and** a field on `ParticipantPayload` (`api/v1/participants.py`) or pydantic drops it before the singleton ever sees it. **`mainCharacter` WAS THE ONE VIOLATION AND IS GONE** (2026-09-15): MSB has no "main" — a team is drafted per game, the captain is the game's answer — so it was a field about a concept the sport does not have, it had no entry in `RESURFACE_MAP`, and the only thing it could ever draw was a caster's sub-plate. `RESURFACE_MAP` is therefore now EXACTLY `display`, which is the property to keep: a `display` key with no scoreboard target is a key nothing can put on air. Removal self-heals both ways — `_merge_block` drops it off a stored row at `Load`, `_normalize_slot` drops an authored `subField` that names it — so neither needed a migration. **The Address Book authors the whole block**, `state` and `youtube` included; a `display` field with no cell on that page is one the start.gg import can set and a producer cannot fix |
| **Address books and league logos** (several books, a league link, logos on air) | `server/participants.py` (`books`, `MAIN_BOOK`, `book_for_mode`/`league_logo`/`resolve_for_mode`, `SetLogo`/`logo_url`, `Export`/`ExportZip`/`ReadZip`/`ImportRows`/`ImportAsNewBook` — `ReadZip` extracts nothing to disk and reads only `book.json` plus the logo paths it names, each size-checked from the zip's own header before it is inflated; `_adopt_team_logos` moves a team-era book's logos onto their players once at load), `server/league_logos.py` (the on-air half), `server/api/v1/participants.py` (`/books…`, `/{pid}/logo`), client `src/context/participants.js` + `src/routes/player_list/books.jsx`, `tests/unit/test_address_books.py`. **Lookup order**: a book-less `MatchByRioName` prefers `main` then any book (so someone kept only in a league book still resurfaces everywhere); a board whose game is in a league's mode resurfaces from THAT book first (`_apply_resurface` → `resolve_for_mode`) — which is how a league row's `prefix` becomes the tag line in its games; a LOGO only ever comes from the book of the league being played. **The logo is a State write HOOK, not a `RESURFACE_MAP` entry** — it is a fact about (person, league), fed by two writers that never meet (the side's `rioName`, from every feed and the Match projector; the board's mode), so `LeagueLogos.on_write` answers both in the same batch, `Settings.watchers` → `on_settings` answers a binding's `stats_tag`, and `reproject_dependents` answers a book edit. It owns `score.{N}.league` + `score.{N}.player.{T}.league_logo`, writes the full set and only what differs. **Which mode**: a producer's manual `stats_tag` pick › the game's `game_mode` › the auto `stats_tag`. On air `league_logo` leads every logo well (`RioData.leagueLogoUrl`, then the MSB team, then the captain). start.gg imports fill `main` only; a shared book always opens as a NEW book. **Pulling a Rio community** (`POST /participants/books/{bid}/community` → `stats_api.fetch_community_roster` → `Participants.AddPlayers`) is additive and copies what `main` already knows about each person; its game modes are the tags carrying the community's `comm_id`, and they fill `book.modes` only when the book has none. **A PRIVATE community (the NNL is one) refuses its member list to a Rio key outside it**, so the roster falls back to everyone who has played in the community's modes (three modes at a time — a burst draws 500s; `NNL Season 4` 500s on Rio's side at any size), and the response's `source` says which answer it was. **Bulk logos** (`LogoUploadDialog`, store `attachLogos`): the ONLY question per image is WHOSE it is — picked by hand, nothing matched from file names, by the producer's call — and a player already picked for another image is not offered again. The league settings are one toolbar row (`LeaguePanel`); a person's logo is the `PlayerLogo` square beside their name in the table (click to replace, × to remove) |
| Competition organizers (address-book references, not free text) | `server/organizers.py` (the projector — **an absent `tournamentInfo.organizers` means never authored and is left alone**, or it would erase legacy hand-typed text), `server/api/v1/organizers.py`, `src/context/organizers.js`, `OrganizerRows` in `src/routes/tournament_info/tournament_info.jsx` (that file is now the competition FORM only — the entrants table moved to `src/routes/competition/entrants.jsx`) |
| Event Header bands (order, visibility, per-field text) | **A band is an ordered LIST, not a run of switches**: `overlays.eventheader.bands.{header,footer}` = `[{id, on, text}]`, drawn left→right, `text` overriding the field's source and blank falling back to it (which is why `message` is just the entry with no source). Model + census in `src/routes/production/eventheader.js` (client) and `EVENTHEADER_FIELDS` / `_eventheader_bands` in `server/settings.py` (migration off the old `show*` switches, and the heal that keeps a field from ending up in no band); drawn by `public/layout/eventheader/eventheader.html` (`fieldSource`); authored as two ribbons on `src/routes/production/stage/eventheader.jsx`. Both bands share ONE settings key so a cross-band move can't be half-confirmed. **Adding a field is one entry in each census** — the heal appends it to its default band on the next boot, and a field with no source draws nothing until someone types in it, so a new one is never intrusive (that is how the `twitter`/`youtube` pair shipped). Those two are `message` plus a MARK: a handle cannot say which platform it is on, so `public/layout/lib/social-marks.js` draws one inside the field's own span — **not** a copy of the theme `<symbol>`s `lib/sub-glyph.js` reaches for Commentary and Player Plates, because the Event Header has no theme SVG for a package to override. `LAYOUT_SETTINGS.eventheader` keeps only what is genuinely a setting — each band's own on/off, the two offsets, a type size PER BAND (`headerFontSize`/`footerFontSize` — `--font-scale` lives on each band, not the stage), and the shared look. **The band IS the plate**: its height is a function of the type size and the row is CENTRED in it, on its CAP BAND rather than its line box — whether a line box's spare space is symmetric is a fact about the face (0.3px off on Inter, 2px on Bebas Neue at 34px), so the cap height is MEASURED per resolved face (`capMetrics`, redone on `fonts.loadingdone`) and published as `--eh-cap` for the social marks' seat, which used to bake Inter's 0.727 while the bands draw Rajdhani (0.643) |
| Post-game callouts (Character Spotlight, Game Summary) | `public/layout/postgame/{spotlight,summary}.html` (their own sources) + `public/layout/lib/postgame-{callout,vs}-mount.js` (shared with the container path via `fed-container.js`'s `MEMBERS`); registered `direct` in `src/routes/production/elements.js`, hostable by virtue of their `container-members.js` entries. Both gate their reveal through `lib/reveal-gate.js` and expose **`setShown`**, which is what the shells and `fed-container` wire `onObsShown` to — a member exposing it is told BOTH edges (and on the way in, so one fed while the container is hidden doesn't burn its reveal on frames nobody composites); a member with only `replay` still gets the off→on call |
| **WHICH CHARACTER the spotlight is about** | `public/layout/lib/spotlight-intent.js` — `resolveSpotlight` = the producer's pick at `production.feed.last.postgamecallout` while it still names a character in the CURRENT capture, else `spotlightSuggestion` (the winning side's leader in total bases). **It lives in the OVERLAY runtime and `src/routes/production/suggest.js` imports it**, the same shape as `container-members.js` — because the console's picker and the element's own OBS source both have to answer this and they answered it differently: the console validated the memory, `spotlight.html` read it RAW. A `charIndex` indexes ONE game's roster, so from the second capture on the panel said “Suggested — Yoshi” while the broadcast drew whoever now sat in that slot, under a comment promising the two could never disagree. One implementation, not two pinned by a parity test. Memory is validated **by name at its index** (so a Bo3 that keeps its rosters keeps the producer's choice) and **per board** (one memory app-wide, a capture per board). A pick payload without `name` is therefore always stale — the sample bundle carries one. `FEED_INTENT` is now one `resolve` per feed kind rather than a `valid`+`suggest` pair, because the composition was the half that was wrong. `suggest.test.js` · `feed-pickers.test.jsx`. The picker's per-character line (`characterSummary`, `feed-pickers.jsx`) is the capture's `batting.line` **verbatim** — `format_batting_line` (`server/rio/pyrio/stat_formatters.py`) already appends every non-zero count stat (HR · 3B · 2B · BB · HBP · RBI · SB, count elided at one), so appending HR and RBI to it printed both twice (`1-for-3, HR, 3 RBI, 1 HR, 3 RBI`); the only fallback is the bare H-for-AB stem, for a capture written before `line` existed, never a second formatter. It is **batting for everyone, pitchers included**: the callout is a batting graphic — its AB Theater replays plate appearances and holds on the spray chart, with pitching a static side panel — so an ERA answers a question the picker is not asking, and one row measured differently makes a nine-row list unscannable for the thing it is scanned for (which is also what the total-bases suggestion ranks) |
| **WHAT A CAPTURE DID TO THE MATCH** | `creditLine` in `src/routes/production/postgame.jsx`, over `match.{m}.credited`. A capture is the one thing on a board that reaches out of `score.{N}` and writes another model — it moves the fixture to `post` and credits the series, which is what DECIDES a Bo1 — and the console stated that nowhere: the fixture's own badge changes on a different row, which shows the result and never the cause, so "a scoreboard decides things in the match" was a rule of the app the producer had never been told. It reads the per-game credit record rather than `decided`, because the question is "did THIS box score do it", and the two come apart in the case worth reporting: a capture naming **no winner** (a quit game reports none) credits nothing and leaves a Bo1 at 0–0, which is the state that reads as "the app forgot" and the only one needing a hand. Amber there, plain otherwise; a caption in the region that performed the write, never a banner |
| Post-game capture | `server/postgame.py` (+ StatFiles path gating), `server/postgame_watch.py` (**auto-capture**: Project Rio writing the stat file IS the end-of-game signal for a local board — the HUD feed has no final frame — gated by `postgame.auto_capture`, default on); console surface is a **region on the board panel** (`src/routes/production/postgame.jsx` → `desks/board.jsx`), never a desk. **THE VERBS RIDE THE REGION'S RULE** (`PostGameActions` in `KitColumn action`, the slot the sides' Swap uses one region up), so a board with nothing captured is ONE ROW and its two captions (`FILE` · `MATCH`) share one wrapping line beneath — two eyebrowed half-sentences on a 1100px panel are two rows each ~85% empty. All three verbs are RECOVERY: the stat file fires the capture on its own and the press a producer actually makes at the end of a game is the turnover bar's Capture, beside the clear and the take it belongs with — so as a row of their own they were the region's loudest thing for the act nobody performs on an ordinary night. **The capture here is never the panel's filled press** (exactly one filled press belongs to a panel and it is the forward move — the clear, or the take; the turnover bar's own Capture is a ghost, so a filled one down here drew the recovery louder than the thing it recovers) **and it is disabled with no game to read**: `find_file` matches the stat file by `score.{N}.game_id` (`server/postgame_files.py`), so on an emptied board that filled button could only ever answer "No game id for this scoreboard yet". The **file picker beside it stays live** — picking by hand captures WITHOUT the game-id match, which is the entire reason that hatch exists |
| Game Summary captain hero art (size/placement/mirror) | `public/layout/lib/captain-framing.js` (measured art + per-side frames) + `src/routes/layouts/captain-framing.test.js` (pins the no-clip / face-clear / one-size contract) |
| Tournament integration | `server/startgg/`, `server/api/v1/startgg.py` |
| Which bracket phase is on air | `src/routes/production/bracket.jsx` (the shared `useBracketDesk` + `BracketPhasePicker`), surfaced on the **source that draws it** (`stage/bracket.jsx`) and the lower-third's bracket slot. One loaded phase app-wide (`bracket.*`); there is no Bracket desk — both consumers already carried the picker |
| Update character data | `user_data/games/msb/base_files/config.json` |
| Add team logo | drop `.png` into `user_data/game_assets/msb/teamLogos/` named after the MSB team |

---

## Performance Guidelines

This app runs alongside the game. **Performance is a hard requirement.**

### Backend

- **Always use `State.SetBatch()` for multi-key writes.** One HUD event touches 30-100+ keys; `Set()` per key emits 30-100+ SocketIO events. SetBatch collapses to one.

  ```python
  # WRONG — 30 SocketIO events
  for key, value in entries:
      await State.Set(key, value)

  # RIGHT — 1 SocketIO event
  await State.SetBatch(entries)
  await State.Save()
  ```

- **Never reintroduce DeepDiff, msgpack, or full-state cloning on the hot path.** Change detection uses the `changed_keys` list; `last_state` is updated selectively.
- **Cache frequently-read settings as class variables** — avoid `await Settings.Get()` in per-HUD-event callbacks.
- **Use indexed lookups** (e.g., `StatsTracker._api_index`) instead of O(n) DataFrame scans.
- **Concurrent awaits: `asyncio.gather()`, not `asyncio.wait()`** — the latter swallows exceptions.

### Frontend

- **Wrap data-connected components in `React.memo()`.**
- **One `useShallow` Zustand selector per component** — combine subscriptions.
- **`useMemo` for derived data passed as props.**

### SocketIO + Overlays

- **Handle both `v1.state.set` and `v1.state.set_batch`** in every consumer (overlays: use `OverlayBase.init`, which guarantees this).
- **`requestAnimationFrame` batching** — events within one frame flush as a single Zustand update.
- **Layouts: apply all batch items to local state first, render once.**

## Known Limitations

- Full state is sent on initial WebSocket connect (incremental after that).
- A second server instance shares `user_data/` and the port unless isolated via the `PRSH_USER_DATA_DIR`/`PRSH_PORT`/`PRSH_NO_BROWSER`/`PRSH_HUD_FILE` env overrides (`server/paths.py`; see the `run-and-verify` skill).
- `pandas` is ~50MB but required by pyrio.
